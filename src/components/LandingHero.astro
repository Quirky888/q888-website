---
import GraphiteMist from "./GraphiteMist.astro";
---

<div class="flex flex-col min-h-screen w-full overflow-x-hidden">
  <!-- 1) Full-bleed image band with Overlay micro-header -->
  <div class="relative w-full h-[clamp(220px,45vh,550px)] overflow-hidden">
    <!-- Hero image (z-0) -->
    <img 
      src="/images/Q888_landining.webp" 
      alt="Q888 Landscape" 
      class="absolute inset-0 z-0 w-full h-full object-cover"
      decoding="async"
      loading="eager"
    />
    
    <!-- Graphite Mist Overlay (z-10) -->
    <GraphiteMist />
    
    <!-- Subtle gradient for text readability (z-15) -->
    <div class="absolute inset-0 z-[15] bg-black/[0.02]"></div>

    <!-- Micro-header overlay (z-20) -->
    <div class="absolute z-20 top-0 left-0 w-full px-6 md:px-24 pt-8 md:pt-12">
      <div class="flex flex-col gap-1">
        <h1 class="text-3xl md:text-5xl font-medium tracking-tight text-neutral-900">Q888</h1>
        <span class="text-xs md:text-sm font-mono text-neutral-400 italic">The hidden d∞oor is slightly open.</span>
      </div>
    </div>
  </div>

  <!-- 3) Main hero copy block (constrained container) -->
  <div class="px-6 md:px-24 py-16 md:py-24 max-w-7xl">
    <div class="space-y-8 md:space-y-12">
      <p class="text-2xl md:text-5xl lg:text-6xl font-light leading-tight md:leading-[1.1] text-neutral-800 max-w-[20ch] md:max-w-[25ch]">
        I'm an artist who builds symbolic systems and narrative tools.
      </p>
      <p class="text-xl md:text-3xl lg:text-4xl font-light leading-relaxed text-neutral-500 max-w-[30ch] md:max-w-[40ch]">
        I make things that don't always explain themselves, but tend to reveal meaning over time.
      </p>
      <p class="text-lg md:text-2xl lg:text-3xl font-light text-neutral-400">
        Welcome to one version of me.
      </p>
    </div>
  </div>
</div>
