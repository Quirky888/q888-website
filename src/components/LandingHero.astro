---
---

<div class="flex flex-col min-h-[100dvh] min-h-screen w-full overflow-x-hidden">
  <div class="relative w-full h-[100dvh] h-screen overflow-hidden bg-[var(--bg-fog)]">
    <picture>
      <source
        type="image/avif"
        media="(max-width: 768px) and (orientation: portrait)"
        srcset="/images/q888_mobile.avif"
      />
      <source
        type="image/webp"
        media="(max-width: 768px) and (orientation: portrait)"
        srcset="/images/q88_mobile_fallback.webp"
      />
      <img
        src="/images/Q888_hero.webp"
        alt="Q888"
        class="absolute inset-0 z-0 w-full h-full object-contain"
        decoding="async"
        loading="eager"
        fetchpriority="high"
        width="800"
        height="1200"
      />
    </picture>

    <span class="hero-tagline-silver hero-tagline-corner tagline-container">
      <span class="tagline-line">The hidden d∞oor</span>
      <span class="tagline-line">is slightly</span>
      <span class="tagline-line">open.</span>
    </span>

    <div class="absolute z-10 left-0 px-6 md:px-24 hero-text-block">
      <h1 class="hero-title-silver">Q888</h1>
    </div>

    <svg width="0" height="0" class="absolute pointer-events-none" aria-hidden="true" focusable="false">
      <defs>
        <filter id="hero-tagline-glitch">
          <feTurbulence type="fractalNoise" baseFrequency="0.02" numOctaves="3" result="noise" />
          <feDisplacementMap in="SourceGraphic" in2="noise" scale="5" />
        </filter>
      </defs>
    </svg>
  </div>

  <div class="landing-typography px-6 md:px-24 pt-12 md:pt-16 pb-16 md:pb-24">
    <div class="landing-status-bar flex flex-col gap-1.5 font-mono text-lg sm:text-xl text-zinc-500 leading-relaxed uppercase tracking-widest mb-12">
      <span class="flex gap-2"><span class="opacity-50">PORTAL_STATE:</span> <hotfx-split-flap class="landing-meta-split-flap">SLIGHTLY_OPEN</hotfx-split-flap></span>
      <span class="flex gap-2"><span class="opacity-50">NARRATIVE_MODE:</span> <hotfx-split-flap class="landing-meta-split-flap">UNFOLDING</hotfx-split-flap></span>
      <span class="flex gap-2"><span class="opacity-50">ENTITY_TYPE:</span> <hotfx-split-flap class="landing-meta-split-flap">IDENTITY</hotfx-split-flap></span>
    </div>
    <p class="landing-line-1">
      Identity &amp; Architecture
    </p>

    <p class="landing-line-2">
      Q888 builds <span class="symbolic-box"><span class="gold-gradient-text">symbolic systems</span></span>, narrative architectures, and speculative tools.
    </p>

    <p class="landing-line-3">
      Operating at the intersection of contemporary art, AI, and conceptual infrastructure, this practice treats myth, code, and interface as equal materials.
    </p>

    <p class="landing-intro-follow">
      This work refuses instant clarity; it is structured to unfold. Meaning accumulates here through repetition, context, and return.
    </p>

    <p class="landing-entry-point">
      <span class="black-box-white">This is not a portfolio. It is an entry point into a set of living systems.</span>
    </p>

    <h2 class="landing-projects-heading">The Systems</h2>

    <p class="landing-paragraph-1">
      <a href="/dink" class="landing-link gold-gradient-text">Digital Ink</a><br />
      <a href="/dink" class="landing-link">AI-driven drawing tools</a> for iPad where image-making becomes a negotiation between human intention and machine interpretation. A research environment for explainable visual collaboration.
    </p>

    <p class="landing-paragraph-2">
      <a href="/map" class="landing-link gold-gradient-text">Edinburgh Magic Map</a><br />
      An interactive mythology layered onto real geography. A cartographic narrative where place becomes interface and story becomes infrastructure.
    </p>

    <p class="landing-paragraph-3">
      <a href="#infocigan" class="landing-link gold-gradient-text">Project Infocigan</a><br />
      A speculative design studio producing artifacts for uncertain futures. Objects released with internal logic, controlled circulation, and conceptual lineage.
    </p>

    <h2 class="landing-method-heading">The Method</h2>

    <p class="landing-method-paragraph">
      Across these works, Q888 constructs ecosystems, not isolated pieces. Editions, protocols, and verification structures are embedded into the code of each project.
    </p>

    <p class="landing-method-paragraph landing-method-paragraph-2">
      Here, scarcity, provenance, and narrative weight are not marketing devices—they are core artistic materials.
    </p>

    <p class="landing-method-paragraph landing-method-paragraph-3">
      <span class="black-box-white">The practice spans screens, walls, and conceptual territories. Technology is treated as substance, not surface. Absurdism is the method used to question value systems, authorship, and the mechanics of belief.</span>
    </p>

    <p class="landing-method-paragraph landing-method-paragraph-4">
      These works behave like operating systems: mythologies with governance, artifacts with lineage, and tools that generate further tools.
    </p>

    <p class="landing-final-line">
      Collaborations, commissions, research partnerships, and critical dialogue are welcome.
    </p>
  </div>
</div>

<script>
  const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)");
  const isTablet = window.matchMedia("(max-width: 1024px)").matches || window.matchMedia("(hover: none)").matches;
  let glitchTimeoutId = 0;

  function runPulseGlitch() {
    const tagline = document.querySelector(".hero-tagline-silver");
    if (!tagline || reducedMotion.matches || isTablet) return;

    const pulseGlitch = () => {
      if (!tagline.isConnected || reducedMotion.matches) return;
      tagline.classList.add("is-glitching");
      window.setTimeout(() => tagline.classList.remove("is-glitching"), 140);
      glitchTimeoutId = window.setTimeout(pulseGlitch, 2000 + Math.random() * 1000);
    };
    pulseGlitch();
  }

  function handleLandingLinkClick(e: MouseEvent) {
    const link = (e.target as HTMLElement).closest(".landing-link") as HTMLAnchorElement;
    if (!link) return;

    const href = link.getAttribute("href");

    // Only intercept hash-only links for smooth scrolling within the page
    if (href && href.startsWith("#") && !href.includes("/")) {
      e.preventDefault();
      const targetId = href.slice(1);
      const targetElement = document.getElementById(targetId);

      if (targetElement) {
        targetElement.scrollIntoView({ behavior: "smooth" });
      }
    }
  }

  function setupListeners() {
    document.querySelectorAll(".landing-link").forEach(link => {
      // Remove existing to avoid duplicates if called multiple times
      link.removeEventListener("click", handleLandingLinkClick as EventListener);
      link.addEventListener("click", handleLandingLinkClick as EventListener);
    });
  }

  function init() {
    runPulseGlitch();
    setupListeners();
  }

  function cleanup() {
    if (glitchTimeoutId) clearTimeout(glitchTimeoutId);
    glitchTimeoutId = 0;
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", init, { once: true });
  } else {
    init();
  }
  document.addEventListener("astro:page-load", init);
  document.addEventListener("astro:before-swap", cleanup);
  window.addEventListener("beforeunload", cleanup);
</script>

<style>
  .hero-tagline-corner {
    position: absolute;
    top: 0;
    left: 0;
    z-index: 10;
    padding: 3.5rem 1.5rem 0 0.75rem;
  }
  @media (max-width: 768px) {
    .hero-tagline-corner {
      padding-top: 5rem;
    }
  }
  @media (min-width: 768px) {
    .hero-tagline-corner {
      padding: 3.75rem 6rem 0 1.5rem;
    }
  }
  .hero-text-block {
    top: 38%;
    transform: translateY(-50%);
  }
  .hero-title-silver {
    font-size: clamp(3.9rem, 12vw, 11rem);
    font-weight: 500;
    letter-spacing: -0.03em;
    background: linear-gradient(
      to right,
      #b0b0b0 20%,
      #e8e8e8 40%,
      #ffffff 50%,
      #e8e8e8 60%,
      #b0b0b0 80%
    );
    background-size: 200% auto;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: hero-shine 8s linear infinite;
  }
  .hero-tagline-silver {
    font-family: ui-monospace, monospace;
    font-size: clamp(1.1rem, 2.5vw, 1.6rem);
    font-style: italic;
    display: inline-block;
    background: linear-gradient(
      to right,
      #a8a8a8 20%,
      #e0e0e0 40%,
      #ffffff 50%,
      #e0e0e0 60%,
      #a8a8a8 80%
    );
    background-size: 200% auto;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: hero-shine 8s linear infinite;
  }
  .tagline-line {
    display: inline;
  }
  @media (max-width: 768px) and (orientation: portrait) {
    .tagline-line {
      display: block;
    }
  }
  .hero-tagline-silver.is-glitching {
    filter: url("#hero-tagline-glitch");
  }
  @keyframes hero-shine {
    to { background-position: 200% center; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-title-silver,
    .hero-tagline-silver {
      animation: none;
    }
  }

  /* Specific adjustment for mobile portrait */
  @media (max-width: 768px) and (orientation: portrait) {
    .hero-text-block {
      top: 33.4%;
    }
    .hero-title-silver {
      font-size: 3.0rem;
    }
  }

  .landing-typography {
    position: relative;
    min-height: 78dvh;
    color: var(--text-ink);
    font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
  }

  .landing-status-bar hotfx-split-flap.landing-meta-split-flap {
    font-family: var(--font-mono, ui-monospace, monospace);
    font-size: 1.125rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #fff;
    --hotfx-split-flap-grid-gap: 0.25em;
  }
  @media (min-width: 640px) {
    .landing-status-bar hotfx-split-flap.landing-meta-split-flap {
      font-size: 1.25rem;
    }
  }
  .landing-status-bar hotfx-split-flap.landing-meta-split-flap::part(char) {
    font-family: var(--font-mono, ui-monospace, monospace);
    font-size: inherit;
    letter-spacing: 0.1em;
    background: #000;
    padding: 0.2em 0.35em;
    border-radius: 0.15em;
  }
  .landing-status-bar hotfx-split-flap.landing-meta-split-flap::part(flap) {
    color: #fff;
    background: transparent;
  }
  .landing-status-bar hotfx-split-flap.landing-meta-split-flap::part(divider) {
    background: rgb(113 113 122);
    opacity: 0.25;
  }

  .landing-line-1 {
    margin: 0;
    margin-left: 6%;
    max-width: 20ch;
    font-size: clamp(2.5rem, 5vw, 4rem);
    line-height: 1.2;
    font-weight: 650;
  }

  .landing-line-2 {
    margin: 2rem 0 0 10%;
    max-width: 28ch;
    font-size: clamp(1.75rem, 3.5vw, 2.75rem);
    line-height: 1.4;
    font-weight: 450;
    opacity: 0.9;
  }

  .landing-line-3 {
    margin: 2.25rem 0 0 62%;
    max-width: 36ch;
    font-size: clamp(1.15rem, 2.2vw, 1.45rem);
    line-height: 1.5;
    font-weight: 400;
    color: var(--text-ink);
    opacity: 0.9;
  }

  .landing-intro-follow {
    margin: 2.1rem 0 0 12%;
    max-width: 60ch;
    font-size: clamp(1.05rem, 1.9vw, 1.3rem);
    line-height: 1.7;
    font-weight: 360;
    opacity: 0.86;
  }

  .landing-entry-point {
    margin: 2.2rem 0 0 54%;
    max-width: 34ch;
    font-size: clamp(1.05rem, 2vw, 1.3rem);
    line-height: 1.55;
    font-weight: 520;
  }

  .landing-projects-heading {
    margin: 5rem 0 0 14%;
    font-size: 0.875rem;
    font-weight: 600;
    color: rgb(184, 143, 98);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    line-height: 1;
  }

  .landing-paragraph-1 {
    margin: 2rem 0 0 10%;
    max-width: 700px;
    font-size: clamp(1.15rem, 2.2vw, 1.5rem);
    line-height: 1.7;
    font-weight: 400;
  }

  .landing-paragraph-2 {
    margin: 3.2rem 0 0 42%;
    max-width: 650px;
    font-size: clamp(1.1rem, 2vw, 1.35rem);
    line-height: 1.7;
    font-weight: 350;
    opacity: 0.85;
  }

  .landing-paragraph-3 {
    margin: 3rem 0 0 12%;
    max-width: 680px;
    font-size: clamp(1.05rem, 1.9vw, 1.28rem);
    line-height: 1.7;
    font-weight: 350;
    opacity: 0.9;
  }

  .landing-method-heading {
    margin: 4.5rem 0 0 16%;
    font-size: 0.875rem;
    font-weight: 600;
    color: rgb(184, 143, 98);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    line-height: 1;
  }

  .landing-method-paragraph {
    margin: 1.6rem 0 0 16%;
    max-width: 680px;
    font-size: clamp(1.05rem, 1.85vw, 1.25rem);
    line-height: 1.75;
    font-weight: 350;
    opacity: 0.9;
  }

  .landing-method-paragraph-2 {
    margin-top: 1.7rem;
    margin-left: 44%;
  }

  .landing-method-paragraph-3 {
    margin-top: 1.7rem;
    margin-left: 12%;
  }

  .landing-method-paragraph-4 {
    margin-top: 1.7rem;
    margin-left: 48%;
  }

  .landing-final-line {
    margin: 3.5rem 0 0 64%;
    max-width: 32ch;
    font-size: clamp(1.15rem, 2.2vw, 1.45rem);
    line-height: 1.6;
    font-weight: 500;
    color: rgb(184, 143, 98);
  }

  .gold-gradient-text {
    background: linear-gradient(135deg, rgb(218, 197, 143) 0%, rgb(184, 143, 98) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 600;
  }

  .landing-link {
    text-decoration: none;
    color: inherit;
    transition: opacity 0.3s ease;
    cursor: pointer;
  }

  .landing-link:hover {
    opacity: 0.8;
  }

  .symbolic-box {
    display: inline-block;
    background: #000;
    padding: 0.24em 0.58em 0.3em 0.48em;
    border-radius: 0.46em 0.22em 0.58em 0.3em;
    line-height: 1;
    transform: rotate(-1.6deg);
  }

  .black-box-white {
    display: inline;
    color: #fff;
    background: #000;
    padding: 0.24em 0.52em 0.3em 0.44em;
    border-radius: 0.5em 0.24em 0.6em 0.28em;
    box-decoration-break: clone;
    -webkit-box-decoration-break: clone;
    line-height: 1.68;
    -webkit-text-fill-color: #fff;
    background-clip: border-box;
    -webkit-background-clip: border-box;
  }

  .landing-entry-point .black-box-white {
    transform: rotate(-1.2deg);
    display: inline-block;
  }

  .landing-method-paragraph-3 .black-box-white {
    transform: rotate(1.35deg);
    display: inline-block;
    padding: 0.26em 0.6em 0.34em 0.5em;
    border-radius: 0.62em 0.26em 0.42em 0.56em;
  }

  @media (max-width: 980px) {
    .landing-typography {
      min-height: auto;
    }

    .landing-line-1,
    .landing-line-2,
    .landing-line-3,
    .landing-intro-follow,
    .landing-entry-point,
    .landing-projects-heading,
    .landing-paragraph-1,
    .landing-paragraph-2,
    .landing-paragraph-3,
    .landing-method-heading,
    .landing-method-paragraph,
    .landing-final-line {
      margin-left: 0;
      margin-right: 0;
      max-width: none;
    }

    .landing-line-2 {
      margin-top: 1.5rem;
    }

    .landing-line-3 {
      margin-top: 1.5rem;
      margin-left: 8%;
    }

    .landing-intro-follow {
      margin-top: 1.5rem;
      margin-left: 4%;
    }

    .landing-entry-point {
      margin-top: 1.6rem;
      margin-left: 10%;
    }

    .landing-projects-heading {
      margin-top: 3rem;
      margin-left: 4%;
    }

    .landing-paragraph-1 {
      margin-top: 1.2rem;
      margin-left: 2%;
    }

    .landing-paragraph-2 {
      margin-top: 2rem;
      margin-left: 9%;
    }

    .landing-paragraph-3 {
      margin-top: 2rem;
      margin-left: 3%;
    }

    .landing-method-heading {
      margin-top: 2.4rem;
      margin-left: 6%;
    }

    .landing-method-paragraph {
      margin-top: 1.2rem;
      margin-left: 4%;
    }

    .landing-method-paragraph-2,
    .landing-method-paragraph-3,
    .landing-method-paragraph-4 {
      margin-left: 9%;
      margin-top: 1.2rem;
    }

    .landing-final-line {
      margin-top: 2.2rem;
      margin-left: 12%;
    }
  }
</style>
