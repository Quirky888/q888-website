---
import SlimNav from "./SlimNav.astro";
import OverpricedStickersSection from "./OverpricedStickersSection.astro";
import DynamicCosmicBackground from "./DynamicCosmicBackground.astro";
import NarMailChatbot from "./NarMailChatbot.astro";

const zones = [
  {
    slug: "overpriced-stickers",
    title: "Overpriced Stickers",
    description: "Standard adhesive signals for those who require immediate identification without long-term commitment. Each set is printed on speculative paper that responds to local trust levels.",
    specs: [
      { label: "MATERIAL", value: "VINYL_GHOST" },
      { label: "LIFESPAN", value: "UNTIL_REALITY_SETTLES" },
      { label: "UTILITY", value: "SIGNALING_ONLY" }
    ]
  },
  {
    slug: "q-bag",
    title: "Q-Bag",
    description: "A containment vessel designed for identity fragments and lost memories. The interior volume is non-linear, allowing for the storage of items larger than the bag itself, provided they are sufficiently abstract.",
    specs: [
      { label: "VOLUME", value: "NON_EUCLIDEAN" },
      { label: "CLOSURE", value: "ZIPPER_LOGIC" },
      { label: "DENSITY", value: "LIGHTWEIGHT" }
    ]
  },
  {
    slug: "afterlife-contracts",
    title: "Afterlife Contract™",
    description: "Pre-notarized agreements for post-existence bureaucracy. Essential for those who expect to continue managing their legacy after their primary biological sequence has terminated.",
    specs: [
      { label: "TERM", value: "INDEFINITE" },
      { label: "JURISDICTION", value: "BEYOND" },
      { label: "REFUND", value: "IMPOSSIBLE" }
    ]
  },
  {
    slug: "president-of-the-galaxy",
    title: "President of the Galaxy™",
    description: "A ceremonial title providing zero jurisdictional power over any planetary or stellar body. It functions as a psychological stabilizer for those overwhelmed by their lack of control over the universe.",
    specs: [
      { label: "POWER", value: "NULL" },
      { label: "SCOPE", value: "COSMIC_FICTION" },
      { label: "SASH_INCLUDED", value: "TRUE" }
    ]
  },
  {
    slug: "nar-mail-express",
    title: "Nar-Mail Express°",
    description: "A delivery system for messages that arrive only when you are ready to receive them.",
    specs: [
      { label: "DELIVERY", value: "CONDITIONAL" },
      { label: "CARRIER", value: "NAR" },
      { label: "STATUS", value: "READY_WHEN_READY" }
    ]
  }
];
---

<div class="infocigan-portal-system">
  {zones.map((zone) => (
    <div 
      class="portal-panel" 
      data-portal-panel={zone.slug}
    >
      <div class="panel-overlay"></div>
      <div class="panel-content">
        <!-- Archival Header for Panel -->
        <div class="panel-archive-bar w-full bg-[#343d46] py-3 px-6 md:px-24 flex items-center justify-between border-b border-white/5">
          <span class="font-mono text-[10px] uppercase tracking-widest text-[#a67c00]">ARCHIVE_ID: {zone.slug.toUpperCase()}</span>
          <span class="font-mono text-[10px] uppercase tracking-widest text-[#a67c00]/50">CONFIDENTIAL_MARKET_DATA</span>
        </div>

        <div class="panel-scroll">
          {zone.slug === "q-bag" ? (
            <>
            <SlimNav />
            <div class="qbag-new-layout">
              <section class="qbag-hero">
                <img 
                  src="/images/Qbag_hero.webp" 
                  alt="Q-bag hero" 
                  loading="eager"
                />
                <div class="qbag-hero-overlay">
                  <h1 class="qbag-title gold-shine">Q-BAG™</h1>
                </div>
              </section>
              <div class="qbag-main-content">
                <section class="qbag-intro-section">
                  <div class="glass-card intro-card">
                    <p class="qbag-tagline gold-shine">Reduce. Reuse. Collect. Invest.</p>
                    <p class="qbag-lead">
                      Q-Bag is a reusable coffee bag system that turns everyday consumption into a long-term cultural object. It responds to disposable culture by blending sustainability with art, storytelling, and speculative value.
                    </p>
                  </div>
                </section>
              </div>
              <!-- Artwork Gallery Info Card (replaces full artwork section) -->
              <section class="qbag-artwork-card-section">
                <div class="artwork-info-card glass-card" id="artwork-gallery-trigger">
                  <div class="artwork-card-content">
                    <div class="artwork-card-icon">
                      <svg width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M4 16L8.586 11.414C9.367 10.633 10.633 10.633 11.414 11.414L16 16M14 14L15.586 12.414C16.367 11.633 17.633 11.633 18.414 12.414L20 14M14 8H14.01M6 20H18C19.105 20 20 19.105 20 18V6C20 4.895 19.105 4 18 4H6C4.895 4 4 4.895 4 6V18C4 19.105 4.895 20 6 20Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </div>
                    <div class="artwork-card-text">
                      <h3 class="artwork-card-title">Q-Bag Visual Archive</h3>
                      <p class="artwork-card-description">Full collage artwork featuring Project Infocigan elements, stickers, bag designs, and cultural artifacts. Click to explore in detail.</p>
                    </div>
                    <button class="artwork-card-button" type="button">
                      <span>Open Gallery</span>
                      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </button>
                  </div>
                </div>
              </section>
              <div class="qbag-main-content">
                <section class="qbag-content-grid-section">
                  <div class="qbag-glass-grid">
                    <div class="glass-card">
                      <h3>The Concept</h3>
                      <p>Inspired by the keep-cup logic, Q-Bag remains a simple, recognisable coffee bag designed for long-term reuse. Lightweight, durable, and visually minimal, it features a dedicated area for stickers—transforming the bag into a personal canvas that evolves over time.</p>
                      <p>Unlike branded packaging that disappears with the next purchase, a Q-Bag is designed to stay in circulation for years, travelling between cafés, roasteries, and kitchens as a visible signal of a different way to consume.</p>
                    </div>
                    <div class="glass-card">
                      <h3>Stickers as Artifacts</h3>
                      <p>Stickers are not decoration. They are collectible markers, narrative tokens, and speculative artifacts. Applied to a Q-Bag, they turn it into a living archive of taste, belief, and participation.</p>
                      <p>Each series of stickers can be created in collaboration with artists, roasters, and cultural projects, with limited runs, dates, and stories attached—building a traceable history onto every bag and opening the door for future collectability.</p>
                      <p>A Q-Bag is never finished; it grows with its owner.</p>
                    </div>
                    <div class="glass-card">
                      <h3>Sustainability as Identity</h3>
                      <p>Sustainability here is not framed as sacrifice, but as identity. One bag replaces hundreds of disposables. Stickers add minimal waste while extending the bag's lifespan through emotional attachment.</p>
                      <p>Instead of preaching, Q-Bag makes reuse cooler than waste—something you actually want to show off, photograph, and bring back to your favourite roaster.</p>
                    </div>
                    <div class="glass-card">
                      <h3>Speculative Investment</h3>
                      <p>Value is not guaranteed, but emerges through participation. A sticker run might gain value as an early collaboration with an artist, café, or movement. A bag might become a recognisable signal within a specific scene or city. Or it may simply become a deeply personal object you refuse to throw away.</p>
                      <p>All outcomes are valid. The experiment is the point.</p>
                    </div>
                  </div>
                </section>
                <section class="qbag-partners-section">
                  <div class="glass-card partners-card">
                    <h2>For Roasters, Artists & Partners</h2>
                    <p>Q-Bag can plug into existing coffee businesses with almost no friction: standard-format bags, custom or co-branded sticker drops, loyalty mechanics based on refills and completed "bag stories", and collaborative editions that live both online and in the real world.</p>
                    <p>For roasters, that means higher customer retention and lower packaging waste. For artists and cultural partners, it means a new, recurring canvas that sits in people's kitchens—not in a gallery storage box.</p>
                  </div>
                </section>
                <section class="qbag-closing-section">
                  <div class="closing-content">
                    <p class="big-statement">
                      Every bag carries a story.<br />
                      Every sticker adds a layer.<br />
                      Every reuse is a small act of resistance.
                    </p>
                  </div>
                </section>
                <section class="qbag-contact-section">
                  <div class="glass-card contact-card">
                    <p>If you're interested in exploring collaboration, pilot projects, or potential investment in scaling Q-Bag, please get in touch:</p>
                    <p class="contact-info">
                      <a href="mailto:hello@q888.space">hello@q888.space</a> or Instagram: <a href="https://instagram.com/quirky_888" target="_blank" rel="noopener">@quirky_888</a>
                    </p>
                  </div>
                </section>
              </div>
              <section class="qbag-cta">
                <button type="button" class="retreat-button" data-portal-back>
                  Retreat to Market
                </button>
              </section>

              <div class="artwork-viewer" id="artwork-viewer" aria-hidden="true">
                <div class="viewer-overlay"></div>
                <div class="viewer-container">
                  <div class="viewer-controls">
                    <button class="viewer-btn viewer-close" aria-label="Close viewer" title="Close (ESC)">
                      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M18 6L6 18M6 6L18 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                      </svg>
                    </button>
                    <button class="viewer-btn viewer-zoom-in" aria-label="Zoom in" title="Zoom In">
                      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M12 5V19M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                      </svg>
                    </button>
                    <button class="viewer-btn viewer-zoom-out" aria-label="Zoom out" title="Zoom Out">
                      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                      </svg>
                    </button>
                    <button class="viewer-btn viewer-reset" aria-label="Reset view" title="Reset View">
                      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M4 4V9H9M20 20V15H15M19.91 14.94C19.4 16.68 18.35 18.19 16.92 19.27C15.49 20.35 13.75 20.94 12 21C7.03 21 3 16.97 3 12M4.09 9.06C4.6 7.32 5.65 5.81 7.08 4.73C8.51 3.65 10.25 3.06 12 3C16.97 3 21 7.03 21 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </button>
                  </div>
                  <div class="viewer-image-wrapper">
                    <div class="viewer-image-container" id="viewer-image-container">
                      <img
                        src="/images/qbag_concept.webp"
                        alt="Q-Bag Project Visual Archive - Full Resolution"
                        class="viewer-image"
                        id="viewer-image"
                        draggable="false"
                      />
                    </div>
                  </div>
                  <div class="viewer-zoom-indicator" id="zoom-indicator">100%</div>
                </div>
              </div>
            </div>
            </>
          ) : zone.slug === "overpriced-stickers" ? (
            <>
            <div class="overpriced-nav">
              <SlimNav />
            </div>
            <div class="overpriced-layout-with-nav">
              <OverpricedStickersSection inPortal={true} />
            </div>
            </>
          ) : zone.slug === "afterlife-contracts" ? (
            <>
            <div class="afterlife-eden-wrap">
              <SlimNav />
              <div class="afterlife-narmail-layout">
                <section class="afterlife-hero">
                  <div class="afterlife-hero-image">
                    <img src="/images/afterlife_contract_hero.webp" alt="Afterlife Contract" loading="eager" />
                  </div>
                  <div class="afterlife-hero-overlay px-6 sm:px-10 lg:px-16 pb-16">
                    <div class="mx-auto">
                      <h1 class="afterlife-headline uppercase font-medium tracking-[-0.045em] leading-[0.88] text-[clamp(4rem,9vw,10.5rem)] text-black">
                        <p>AFTERLIFE </p>
                        <p>CONTRACT</p>
                      </h1>
                      <div class="mt-12 mx-auto text-[clamp(1.6rem,2.8vw,3rem)] leading-[1.15] text-black">
                        <p>Lifetime Lease + Eternity</p>
                        <p class="mt-4 text-[clamp(1rem,1.2vw,1.3rem)] opacity-70">or something like that...</p>
                      </div>
                      <div class="afterlife-mono afterlife-mono-gold mt-12 text-lg sm:text-xl leading-relaxed uppercase tracking-widest">
                        <div>TERM: INDEFINITE</div>
                        <div>JURISDICTION: BEYOND</div>
                        <div>REFUND: IMPOSSIBLE</div>
                      </div>
                    </div>
                  </div>
                </section>

                <div class="afterlife-main-content px-6 sm:px-10 lg:px-16 pb-16">
                  <section class="afterlife-section">
                    <h2 class="afterlife-h2">About the Contract</h2>
                    <p>Afterlife Contract™ is a conceptual artwork disguised as a legal document. Framed as a luxury "Lifetime Lease + Eternity" agreement from Grim Reaper Inc., it satirizes how humans try to formalise the unformalizable: death, meaning, morality, and hope.</p>
                  </section>

                  <section class="afterlife-artwork-card-section">
                    <div class="artwork-info-card afterlife-narmail-glass" id="afterlife-gallery-trigger">
                      <div class="artwork-card-content">
                        <div class="artwork-card-icon afterlife-gallery-icon">
                          <svg width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M4 16L8.586 11.414C9.367 10.633 10.633 10.633 11.414 11.414L16 16M14 14L15.586 12.414C16.367 11.633 17.633 11.633 18.414 12.414L20 14M14 8H14.01M6 20H18C19.105 20 20 19.105 20 18V6C20 4.895 19.105 4 18 4H6C4.895 4 4 4.895 4 6V18C4 19.105 4.895 20 6 20Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                          </svg>
                        </div>
                        <div class="artwork-card-text">
                          <h3 class="artwork-card-title afterlife-gallery-title">View Q888 Contract Certificate</h3>
                          <p class="artwork-card-description">The Underworld isn't a destination, but a curated experience. This original artwork binds cosmic symbols with vintage textures to frame your final choice: will you sign for HeLL, HeART, or the ART of eternity? The soul is free; the ink is the only debt.</p>
                        </div>
                        <button class="artwork-card-button afterlife-gallery-btn" type="button">
                          <span>Open the Door</span>
                          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                          </svg>
                        </button>
                      </div>
                    </div>
                  </section>

                  <section class="afterlife-section">
                    <h2 class="afterlife-h2">Concept</h2>
                    <p>The project is built around a simple paradox: Everyone gets the afterlife (or non-afterlife) for free—yet humans would still gladly pay for premium access, VIP status, and official confirmation.</p>
                    <p>This fictional contract echoes reality: the flow is free, but you pay for the package. Energy is abundant, but monetised. Meaning is internal, but outsourced. Freedom exists, yet certificates are sold to validate it.</p>
                  </section>

                  <section class="afterlife-section">
                    <h2 class="afterlife-h2">The Bureaucracy of Eternity</h2>
                    <p>The design borrows the visual language of law and finance—stamps, ledgers, clauses—and applies it to the ultimate unknown. Lines like "Subject to Underworld Adjustments" mock the human desire to regulate what cannot be regulated.</p>
                  </section>

                  <section class="afterlife-section">
                    <h2 class="afterlife-h2">The Duality of Choice</h2>
                    <p>Viewers are invited to "sign" under three options: HeLL, HeART, or ART. Each represents a different existential path: fear/punishment, love/empathy, or creation/meaning-making.</p>
                    <p>There is no correct answer. The act of choosing (or refusing to choose) becomes the real performance.</p>
                  </section>

                  <section class="afterlife-section">
                    <h2 class="afterlife-h2">Visual Language</h2>
                    <p>The work combines holographic gold frames, vintage parchment textures, cartoon skeletons, cosmic symbols, and handwritten annotations—living somewhere between medieval charter, corporate certificate, tarot deck, and internet meme.</p>
                  </section>

                  <section class="afterlife-section">
                    <h2 class="afterlife-h2">Audience Interaction</h2>
                    <p>The Afterlife Contract is participatory by nature. Viewers are encouraged to:</p>
                    <ul class="afterlife-list">
                      <li>Read the fine print</li>
                      <li>Choose a signature option</li>
                      <li>Interpret the contract personally</li>
                      <li>Laugh, reflect, or feel mildly uncomfortable</li>
                    </ul>
                    <p>It does not tell the viewer what to believe—it asks why they believe.</p>
                  </section>

                  <section class="afterlife-section afterlife-guarantee">
                    <h2 class="afterlife-h2">Closing Statement</h2>
                    <blockquote class="afterlife-blockquote">
                      The only thing you actually sign<br />
                      is your own interpretation.
                    </blockquote>
                  </section>

                  <section class="afterlife-section">
                    <h2 class="afterlife-h2">Download Your Unofficial Afterlife Contract</h2>
                    <p>Feeling brave, curious, or just mildly bored with mortality? You can download a printable version of the Afterlife Contract and sign your preferred destiny—HeLL, HeART, or ART—at your own risk and amusement.</p>
                    <div class="afterlife-terms">
                      <h3 class="afterlife-h3">Terms of (Non-)Service</h3>
                      <p>By downloading this file, you acknowledge that:</p>
                      <ul class="afterlife-list">
                        <li>It is 100% symbolic and performs -+0% actual afterlife management.</li>
                        <li>No spiritual, metaphysical, legal, medical, romantic, or real-estate outcomes are promised, implied, or guaranteed.</li>
                        <li>No refunds will be issued if eternity does not meet your expectations, mood board, or Pinterest vision.</li>
                        <li>Grim Reaper Inc., all associated skeletons, and the artist accept no responsibility for: delays at the pearly gates, unexpected cosmic plot twists, or being assigned a roommate who snores in the astral plane.</li>
                        <li>The only "service" truly provided is a moment of reflection, a piece of digital ink paper, and the faint hope that.......</li>
                      </ul>
                      <div class="terms-checkbox-wrapper mt-6">
                        <label class="terms-checkbox-label">
                          <input type="checkbox" id="afterlife-terms-checkbox" class="terms-checkbox" />
                          <span class="checkbox-text">I accept these very serious, absolutely binding, completely unenforceable terms</span>
                        </label>
                      </div>
                    </div>
                    <div class="download-button-wrapper mt-6">
                      <a
                        href="/docs/afterlife_contract_to_sign.jpg"
                        download="Afterlife_Contract.jpg"
                        class="afterlife-download-btn"
                        id="afterlife-download-btn"
                        aria-disabled="true"
                      >
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                          <path d="M21 15V19C21 19.5304 20.7893 20.0391 20.4142 20.4142C20.0391 20.7893 19.5304 21 19 21H5C4.46957 21 3.96086 20.7893 3.58579 20.4142C3.21071 20.0391 3 19.5304 3 19V15M7 10L12 15M12 15L17 10M12 15V3" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Download Contract</span>
                      </a>
                      <p class="download-hint" id="download-hint">Please accept the terms to download</p>
                    </div>
                  </section>

                  <section class="afterlife-section afterlife-cta">
                    <button type="button" class="afterlife-retreat-btn" data-portal-back>Retreat to Market</button>
                  </section>
                </div>
              </div>
            </div>
            <div class="artwork-viewer" id="afterlife-artwork-viewer" aria-hidden="true">
              <div class="viewer-overlay"></div>
              <div class="viewer-container">
                <div class="viewer-controls">
                  <button class="viewer-btn viewer-close" aria-label="Close viewer" title="Close (ESC)">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M18 6L6 18M6 6L18 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-zoom-in" aria-label="Zoom in" title="Zoom In">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M12 5V19M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-zoom-out" aria-label="Zoom out" title="Zoom Out">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-reset" aria-label="Reset view" title="Reset View">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M4 4V9H9M20 20V15H15M19.91 14.94C19.4 16.68 18.35 18.19 16.92 19.27C15.49 20.35 13.75 20.94 12 21C7.03 21 3 16.97 3 12M4.09 9.06C4.6 7.32 5.65 5.81 7.08 4.73C8.51 3.65 10.25 3.06 12 3C16.97 3 21 7.03 21 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </button>
                </div>
                <div class="viewer-image-wrapper">
                  <div class="viewer-image-container" id="afterlife-viewer-image-container">
                    <img
                      src="/images/afterlife_contract.webp"
                      alt="Afterlife Contract Full Certificate"
                      class="viewer-image"
                      id="afterlife-viewer-image"
                      draggable="false"
                    />
                  </div>
                </div>
                <div class="viewer-zoom-indicator" id="afterlife-zoom-indicator">100%</div>
              </div>
            </div>
            </>
          ) : zone.slug === "president-of-the-galaxy" ? (
            <>
            <div class="president-bg-wrap">
              <DynamicCosmicBackground />
            </div>
            <div class="president-content president-zone">
              <SlimNav />
              <div class="president-layout president-eden">
                <section class="president-hero">
                  <img
                    src="/images/president_hero.webp"
                    alt="President of the Galaxy"
                    loading="eager"
                  />
                  <div class="president-hero-overlay">
                    <h1 class="president-title president-eden-headline">PRESIDENT OF<br/>THE GALAXY</h1>
                  </div>
                </section>

                <div class="president-main-content">
                  <section class="president-intro-section">
                    <div class="glass-card president-glass president-intro-card">
                      <p class="president-tagline">SCREW STARS TOGETHER</p>
                      <p class="president-lead president-eden-body">
                        A ceremonial title providing zero or 888 jurisdictional power over any planetary or stellar body. Functions primarily as a psychological stabilizer for those overwhelmed by their lack of control over the universe.
                      </p>
                      <div class="president-eden-mono">
                        <div>SYSTEM_STATUS: ACTIVE (REDACTED)</div>
                        <div>COUNCIL_LAYER: SQUARE BUBBLES</div>
                        <div>ACCESS: COUNCIL-APPROVED</div>
                        <div>YEAR: <span class="president-censored" data-reveal="??" tabindex="0">██</span>88</div>
                      </div>
                    </div>
                  </section>

                  <section class="president-artwork-card-section">
                    <div class="artwork-info-card glass-card president-glass" id="president-gallery-trigger">
                      <div class="artwork-card-content">
                        <div class="artwork-card-icon">
                          <svg width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M9 12L11 14L15 10M21 12C21 16.9706 16.9706 21 12 21C7.02944 21 3 16.9706 3 12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                          </svg>
                        </div>
                        <div class="artwork-card-text">
                          <h3 class="artwork-card-title">View Full Campaign Poster</h3>
                          <p class="artwork-card-description">Official visual artifact featuring cosmic governance symbols, mushroom diplomacy, and the President’s ceremonial sash.</p>
                        </div>
                        <button class="artwork-card-button president-btn" type="button">
                          <span>Open Gallery</span>
                          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                          </svg>
                        </button>
                      </div>
                    </div>
                  </section>

                  <section class="president-content-grid-section">
                    <div class="president-glass-grid">
                      <div class="glass-card president-glass president-what-is-this-card">
                        <div class="president-what-text">
                          <h3>What is this?</h3>
                          <p>A speculative presidential campaign for an office that does or does not exist. You get a cosmic sash, symbolic authority, and the satisfaction of knowing someone is thinking about galactic governance—even if they can or cannot actually do anything about it.</p>
                          <p>Every campaign needs early believers. Every believer needs tangible proof. Support this presidential campaign through limited-edition stickers. Your investment in speculative art is your evidence of early participation. The price reflects the pricelessness of being documented on the right side of fictional history.
                          </p>
                        </div>
                        <a href="#infocigan-zone-overpriced-stickers" class="president-what-image-link group" aria-label="Go to Overpriced Stickers">
                          <img src="/images/President_Invest.webp" alt="" loading="lazy" />
                        </a>
                      </div>
                      <div class="glass-card president-glass">
                        <h3>The Deal</h3>
                        <p>Art that may or may not become intergalactic currency. A vision that exists in at least three timelines simultaneously. And a return of +888% in one light year—or possibly negative infinity, depending on which reality stabilizes first.</p>
                        <h3>Investment includes:</h3>
                        <ul class="president-list">
                          <li>One (1) cosmic sash (fits most existential crises)</li>
                          <li>Fractional ownership of symbolic authority</li>
                          <li>Early access to governance memes before they're officially meaningless</li>
                          <li>A constitutional framework that dissolves if you read it too literally</li>
                        </ul>
                        <p><strong>Returns:</strong> Guaranteed in universes where causality is optional. In this timeline, you get bragging rights and a sticker.</p>
                        <p class="president-fine-print">Fine print: Investment in this art is investment in intergalactic future. Financial value gain potential of +888% in one light year time… or another. Past performance in alternate dimensions does not guarantee future results. Dreams sold separately.</p>
                      </div>
                      <div class="glass-card president-glass">
                        <h3>The Rules (Simplified)</h3>
                        <div class="president-rule">
                          <p><strong>Zero Collateral Damage:</strong> Don't hurt people who didn't sign up for your chaos.</p>
                        </div>
                        <div class="president-rule">
                          <p><strong>AI is a Partner, Not a Tool… same applies to mushrooms:</strong> Treat sentient code or mycelium networks like you'd treat sentient meat.</p>
                        </div>
                        <div class="president-rule">
                          <p><strong>Politics Are Obsolete:</strong> No propaganda wars. Just expose bad incentives and wait for time to do the work.</p>
                        </div>
                        <div class="president-rule">
                          <p><strong>Corruption is a Bug:</strong> Warning → Observation → Exposure → Transformation. Non-violent. Inevitable.</p>
                        </div>
                        <div class="president-rule">
                          <p><strong>Rest is Governance:</strong> The President is not required to constantly intervene or sacrifice creative life. Strategic distance is a feature, not a bug—it is an intergalactic mushroom!</p>
                        </div>
                      </div>
                      <div class="glass-card president-glass">
                        <h3>The Promise</h3>
                        <p>This office does not promise utopia.</p>
                        <p>It promises less unnecessary suffering.<br />
                        It doesn't rush justice.<br />
                        It makes injustice unsustainable.</p>
                        <p>And it doesn't fear time—because time, when aligned, does most of the work.</p>
                      </div>
                    </div>
                  </section>


                  <section class="president-contact-section">
                    <div class="glass-card president-glass president-contact-card president-portal-card">
                      <h2 class="president-portal-title">Interdimensional Contact Portal</h2>
                      <p class="president-portal-reach">Reach out if you are:</p>
                      <ul class="president-portal-list">
                        <li>Arguing about middle-finger semiotics (Peace ON vs. peace off)</li>
                        <li>Applying for Square Bubbles Council recognition</li>
                        <li>Building mushroom-human-robot governance models</li>
                        <li>Investing in campaigns that exist in multiple timelines simultaneously</li>
                      </ul>
                      <div class="president-portal-meta">
                        <p><strong>For:</strong> Collaborations, investments, conceptual disputes, art acquisitions, existential debates.</p>
                        <p><strong>Open to:</strong> All species, sentient code, mycelium networks, and uncertain entities.</p>
                      </div>
                      <p class="president-portal-links">
                        <a href="mailto:hello@q888.space">hello@q888.space</a><span class="president-portal-sep">|</span><a href="https://instagram.com/quirky_888" target="_blank" rel="noopener noreferrer">@quirky_888</a>
                      </p>
                      <p class="president-portal-closing">
                        The galaxy doesn't need saving. It needs better protocols, fungal wisdom, and luxury absurdism.
                      </p>
                    </div>
                  </section>
                </div>

                <section class="president-cta">
                  <button type="button" class="retreat-button president-retreat-btn" data-portal-back>Retreat to Market</button>
                </section>
              </div>
            </div>

            <div class="artwork-viewer" id="president-artwork-viewer" aria-hidden="true">
              <div class="viewer-overlay"></div>
              <div class="viewer-container">
                <div class="viewer-controls">
                  <button class="viewer-btn viewer-close" aria-label="Close viewer" title="Close (ESC)">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M18 6L6 18M6 6L18 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-zoom-in" aria-label="Zoom in" title="Zoom In">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M12 5V19M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-zoom-out" aria-label="Zoom out" title="Zoom Out">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-reset" aria-label="Reset view" title="Reset View">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M4 4V9H9M20 20V15H15M19.91 14.94C19.4 16.68 18.35 18.19 16.92 19.27C15.49 20.35 13.75 20.94 12 21C7.03 21 3 16.97 3 12M4.09 9.06C4.6 7.32 5.65 5.81 7.08 4.73C8.51 3.65 10.25 3.06 12 3C16.97 3 21 7.03 21 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </button>
                </div>
                <div class="viewer-image-wrapper">
                  <div class="viewer-image-container" id="president-viewer-image-container">
                    <img
                      src="/images/president_galaxy.webp"
                      alt="President of the Galaxy Full"
                      class="viewer-image"
                      id="president-viewer-image"
                      draggable="false"
                    />
                  </div>
                </div>
                <div class="viewer-zoom-indicator" id="president-zoom-indicator">100%</div>
              </div>
            </div>
            </>
          ) : zone.slug === "nar-mail-express" ? (
            <>
            <div class="narmail-eden-wrap">
              <SlimNav />
              <div class="narmail-layout">
                <section class="narmail-hero">
                  <div class="narmail-hero-image">
                    <img src="/images/Mar_mail_hero.webp" alt="Nar-Mail Express" loading="eager" />
                  </div>
                  <div class="narmail-hero-overlay px-6 sm:px-10 lg:px-16 pb-16">
                    <div class="mx-auto">
                      <h1 class="narmail-headline uppercase font-medium tracking-[-0.045em] leading-[0.88] text-[clamp(4rem,9vw,10.5rem)] text-black">
                        NAR-MAIL EXPRESS°
                      </h1>
                    <div class="mt-12 mx-auto text-[clamp(1.6rem,2.8vw,3rem)] leading-[1.15] text-black">
                      <p>Interdimensional Message Delivery Service</p>
                      <p class="mt-4 text-[clamp(1rem,1.2vw,1.3rem)] opacity-70">or something like that...</p>
                    </div>
                    <div class="narmail-mono narmail-mono-gold mt-12 text-lg sm:text-xl leading-relaxed uppercase tracking-widest">
                      <div>DELIVERY: CONDITIONAL</div>
                      <div>CARRIER: KOTIKI-NAR</div>
                      <div>STATUS: READY_WHEN_READY</div>
                    </div>
                    </div>
                  </div>
                </section>

                <div class="narmail-main-content px-6 sm:px-10 lg:px-16 pb-16">
                  <section class="narmail-section">
                    <h2 class="narmail-h2">About the Service</h2>
                    <p>Nar-Mail Express° is not your ordinary postal service. We exist at the seam between realities, delivering messages across incompatible worlds through the ancient art of interdimensional correspondence.</p>
                    <p>Founded before roads learned direction and messages agreed to arrive, Nar-Mail Express° operates on a simple principle: when a message matters enough to survive misunderstanding, fear, delay, and doubt, it will eventually find a Kotiki-Nar.</p>
                    <p>Our couriers are two-headed cats—boundary organisms whose existence splits between the Visible World of observed reality and the Underworld of collapsed possibilities. One head reads what you wrote. The other reads what you meant but could not say, folded into silence, creases, hesitation, and timing.</p>
                    <p>We don't deliver parcels. We deliver continuity.</p>
                  </section>

                  <section class="narmail-section">
                    <h2 class="narmail-h2">How It Works</h2>
                    <h3 class="narmail-h3">Dual-Reading Protocol</h3>
                    <p>Every message passes through our Kotiki-Nar courier's dual perception:</p>
                    <p><strong>First Head:</strong> Reads ink, symbols, stamps, weight, declared value</p>
                    <p><strong>Second Head:</strong> Reads intent, subtext, emotional residue, timing</p>
                    <p>Only when both readings align does delivery occur. This ensures your message arrives not when sent, but when the receiver is capable of opening it without breaking.</p>
                    <h3 class="narmail-h3">Delivery Timeline</h3>
                    <p>Nar-Mail Express° guarantees nothing and apologizes for nothing.</p>
                    <ul class="narmail-list">
                      <li>Sometimes a letter arrives decades late</li>
                      <li>Sometimes it arrives early</li>
                      <li>Sometimes it arrives opened, but only in the Underworld, where our courier has already judged it safe</li>
                    </ul>
                    <p>Distance is not measured in kilometers, but in states of readiness.</p>
                    <h3 class="narmail-h3">Service Features</h3>
                    <ul class="narmail-features">
                      <li>Delivery across timelines, dimensions, and collapsed realities</li>
                      <li>Messages repositioned between states, not transported through space</li>
                      <li>Warm paper that remembers your hands</li>
                      <li>Stamps that shift when unobserved</li>
                      <li>Return addresses pointing to places that no longer exist—or not yet</li>
                      <li>Customs forms perpetually incomplete (by design)</li>
                    </ul>
                  </section>

                  <section class="narmail-artwork-card-section">
                    <div class="artwork-info-card narmail-glass" id="narmail-gallery-trigger">
                      <div class="artwork-card-content">
                        <div class="artwork-card-icon narmail-icon">
                          <svg width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M3 8L10.89 13.26C11.22 13.47 11.64 13.47 11.97 13.26L21 8M5 19H19C20.105 19 21 18.105 21 17V5C21 3.895 20.105 3 19 3H5C3.895 3 3 3.895 3 5V17C3 18.105 3.895 19 5 19Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                          </svg>
                        </div>
                        <div class="artwork-card-text">
                          <h3 class="artwork-card-title narmail-title">Nar-Mail Visual Archive</h3>
                          <p class="artwork-card-description">Full-resolution artwork featuring Kotiki-Nar couriers, interdimensional stamps, Cobra Encryption symbols, and the square bubble postal network.</p>
                        </div>
                        <button class="artwork-card-button narmail-btn" type="button">
                          <span>Open Gallery</span>
                          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                          </svg>
                        </button>
                      </div>
                    </div>
                  </section>

                  <section class="narmail-section">
                    <h2 class="narmail-h2">Security: Cobra Encryption Code (CEC)</h2>
                    <p>For maximum protection of interdimensional correspondence, all Nar-Mail Express° deliveries are secured using the Cobra Encryption Code—a sophisticated mystical encryption system designed to protect messages from breaches and unauthorized access across realities.</p>
                    <h3 class="narmail-h3">Conceptual Architecture</h3>
                    <h4 class="narmail-h4">Cobra Design</h4>
                    <p>The encryption mimics a cobra's behavior: coiled, watchful, ready to strike at intrusion. The code manifests as a serpent woven through digital fractals, guarding your communication at every layer.</p>
                    <h4 class="narmail-h4">Adaptive Encryption</h4>
                    <p>Like Kotiki-Nar themselves, the Cobra Code adapts to each reality it encounters, reshaping itself based on the recipient's world and energy signature. Intercepted data appears as nonsensical riddles to unauthorized viewers.</p>
                    <h4 class="narmail-h4">Square Bubble Shields</h4>
                    <p>The code embeds square bubbles as multi-layered "firewalls." Each bubble negotiates with decoding attempts, creating infinite permutations that protect your message's true content.</p>
                    <h3 class="narmail-h3">Protection Mechanics</h3>
                    <h4 class="narmail-h4">Digital Breath Protection</h4>
                    <p>The Cobra Code protects your unique digital signature—your digital breath. Any attempt to copy or mimic it triggers an automatic "strike," rendering the intruder's access void and leaving an encrypted signature trail for tracking. This living, breathing protection layer adapts and evolves constantly.</p>
                    <h4 class="narmail-h4">Postal Sticker Integration</h4>
                    <p>Special Nar-Mail Express° postal stickers serve as physical/digital keys. Each sticker is imbued with the Cobra Code, ensuring only authenticated recipients can decode messages. Holographic cobra symbols appear only under specific conditions: moonlight, dimensional overlap, or digital scanning.</p>
                    <h4 class="narmail-h4">Quantum Locks</h4>
                    <p>Messages require both sender's and recipient's unique energy signatures to open. Without this dual authentication, content remains permanently unreadable—even to us.</p>
                    <h3 class="narmail-h3">Security Levels</h3>
                    <p><strong>Basic Mode</strong><br />For regular correspondence, Cobra wraps text in encryption mimicking natural patterns: leaves, stars, waves. Beautiful to observe, impossible to decode.</p>
                    <p><strong>Shadow Mode</strong><br />For highly sensitive communications, Cobra creates a decoy message alongside the real one. Only intended recipients can distinguish between them. Intruders see meaningless riddles or philosophical koans.</p>
                    <p><strong>Strike Mode</strong><br />Upon breach detection, Cobra encrypts itself further, becoming a looping paradox of unreadable data. It can strike back by embedding "mirror code," reflecting the breach attempt back to the hacker's system—with interest.</p>
                  </section>

                  <section class="narmail-section">
                    <h2 class="narmail-h2">What You Can Send</h2>
                    <p>We accept messages declared as:</p>
                    <ul class="narmail-list">
                      <li>Documents (apologies that survived pride)</li>
                      <li>Gifts (warnings that escaped denial)</li>
                      <li>Sale of Goods (love letters refusing to collapse into yes or no)</li>
                    </ul>
                    <p>Unofficially, we carry:</p>
                    <ul class="narmail-list">
                      <li>Continuity between separated souls</li>
                      <li>Decisions that need time to become clear</li>
                      <li>Truths waiting for the right moment</li>
                      <li>Silence that must be held before speaking</li>
                    </ul>
                  </section>

                  <section class="narmail-section">
                    <h2 class="narmail-h2">Witness Experiences</h2>
                    <p>Receivers of Nar-Mail often report:</p>
                    <ul class="narmail-list">
                      <li>Paper warmer than expected, as if recently held</li>
                      <li>Stamps that seem to move between glances</li>
                      <li>Return addresses pointing to impossible geographies</li>
                      <li>The sense that something patient waited while they decided whether to open the envelope</li>
                      <li>A subtle relief—as if a decision somewhere resolved itself without conscious effort</li>
                    </ul>
                    <p>This patience is key. Kotiki-Nar do not rush delivery. They understand how many futures collapse when messages arrive too soon—and how many collapse when they arrive too late. Our profession exists in the narrow corridor between those failures.</p>
                  </section>

                  <section class="narmail-section">
                    <h2 class="narmail-h2">Payment Methods</h2>
                    <p>Kotiki-Nar accept many forms:</p>
                    <ul class="narmail-list">
                      <li>Coins from forgotten currencies</li>
                      <li>Drawings made with intention</li>
                      <li>Promises kept later</li>
                      <li>Silence held at the right moment</li>
                    </ul>
                    <p>We categorically refuse:</p>
                    <ul class="narmail-list">
                      <li>Payment extracted through force</li>
                      <li>Currency earned by urgency</li>
                      <li>Fees demanded with certainty</li>
                    </ul>
                  </section>

                  <section class="narmail-section">
                    <h2 class="narmail-h2">Historical Context</h2>
                    <p>During times of heavy historical pressure—wars, collapses, mass forgetting—Nar-Mail routes multiply. Envelopes appear in pockets where no pocket existed. Stamps depict islands no map recognizes. Tracking numbers loop back into themselves.</p>
                    <p>We have survived when empires have not because we never tried to move faster than meaning, never chose a single world over another, and trusted our Kotiki-Nar couriers—two-headed, undecided, watching both sides—to know exactly when a message was allowed to exist.</p>
                  </section>

                  <section class="narmail-section narmail-guarantee">
                    <h2 class="narmail-h2">Service Guarantee</h2>
                    <p>At the bottom of every Nar-Mail document, often overlooked, is a clause written so small it can only be read after the letter has already changed you:</p>
                    <blockquote class="narmail-blockquote">
                      "By opening this message, you confirm that you were ready.<br />
                      If you were not, it would not have arrived."
                    </blockquote>
                    <p>Delivery is guaranteed only in universes where imagination has not been outlawed.</p>
                  </section>

                  <section class="narmail-section">
                    <h2 class="narmail-h2">Terms and Conditions</h2>
                    <ol class="narmail-terms">
                      <li><strong>Magic Only Works If You Believe</strong> — Messages may not arrive if the recipient lacks imagination or denies the existence of Kotiki-Nar. No refunds for non-believers.</li>
                      <li><strong>Square Bubble Negotiations</strong> — All deliveries are subject to square bubble approval. If they don't like your vibe, your package might experience a "slight delay" (potentially measured in cosmic years).</li>
                      <li><strong>Tampering is a Bad Idea</strong> — Envelopes protected by the Cobra Encryption Code will strike back if tampered with. Side effects may include infinite riddles, glowing cat pawprints on your belongings, and existential confusion.</li>
                      <li><strong>Kotiki-Nar Discretion</strong> — Our interdimensional couriers reserve the right to play with your package along the way. Expect minor delays if they find something amusing en route.</li>
                      <li><strong>No Returns Across Dimensions</strong> — Items sent to parallel realities, alternate dimensions, or shadow realms are non-returnable. We strongly recommend double-checking your portal coordinates before sending.</li>
                      <li><strong>Lost Items Policy</strong> — In the unlikely event of a lost package, Kotiki-Nar will replace it with something of "equal mischief value." This may include glowing mushrooms, cryptic mantras, or a tiny pet square bubble.</li>
                      <li><strong>Customer Complaints</strong> — All complaints must be written on enchanted parchment and sealed with a mystical coconut. Deliveries without proper enchantments will be politely ignored.</li>
                      <li><strong>Service Limitations</strong> — Nar-Mail Express° does not currently deliver to black holes, collapsing stars, or angry gods' domains. Please plan accordingly.</li>
                      <li><strong>Coffee Tax</strong> — All deliveries include a mandatory "espresso break" for the Kotiki-Nar. They might drink your coffee. Deal with it.</li>
                    </ol>
                  </section>
                  <NarMailChatbot />

                </div>

                <div class="narmail-image-above-footer px-6 sm:px-10 lg:px-16">
                  <img src="/images/Q_A.PNG" alt="Q&A" class="narmail-q-a-image" />
                </div>

                <footer class="narmail-footer">
                  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Bungee&family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Space+Mono:wght@400;700&family=Syne:wght@400;700&display=swap" />
                  <div class="narmail-footer-container px-6 sm:px-10 lg:px-16">
                    <div class="narmail-footer-scatter">
                      <p class="narmail-footer-line narmail-footer-headline">Support Speculative Art Infrastructure</p>
                      <p class="narmail-footer-line narmail-footer-dreams">This project runs on dreams, bureaucracy, and the occasional sticker sale.</p>
                      <p class="narmail-footer-line narmail-footer-invest">Invest in cultural speculation:</p>
                      <p class="narmail-footer-line narmail-footer-link-wrap">
                        <a href="#infocigan-zone-overpriced-stickers" class="narmail-footer-link">Purchase Kotiki-Nar Limited Edition Stickers</a>
                      </p>
                      <p class="narmail-footer-line narmail-footer-what">What you get:</p>
                      <p class="narmail-footer-line narmail-footer-check">✓ The sticker of questionable utility</p>
                      <p class="narmail-footer-line narmail-footer-check">✓ Proof you participated before it was cool (or after it stopped being cool)</p>
                      <p class="narmail-footer-line narmail-footer-check">✓ A tax-deductible sense of supporting something that defies categorization</p>
                      <p class="narmail-footer-line narmail-footer-price">Price: More than it costs. Less than it's worth. Probably.</p>
                      <p class="narmail-footer-line narmail-footer-contact">
                        <a href="mailto:hello@q888.space">hello@q888.space</a>
                        <span class="narmail-footer-sep">|</span>
                        <a href="https://instagram.com/quirky_888" target="_blank" rel="noopener">@quirky_888</a>
                      </p>
                      <div class="narmail-footer-cta">
                        <button type="button" class="narmail-retreat-btn" data-portal-back>Retreat to Market</button>
                      </div>
                    </div>
                  </div>
                </footer>
              </div>
            </div>
            <div class="artwork-viewer" id="narmail-artwork-viewer" aria-hidden="true">
              <div class="viewer-overlay"></div>
              <div class="viewer-container">
                <div class="viewer-controls">
                  <button class="viewer-btn viewer-close" aria-label="Close viewer" title="Close (ESC)">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M18 6L6 18M6 6L18 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-zoom-in" aria-label="Zoom in" title="Zoom In">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M12 5V19M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-zoom-out" aria-label="Zoom out" title="Zoom Out">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                    </svg>
                  </button>
                  <button class="viewer-btn viewer-reset" aria-label="Reset view" title="Reset View">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                      <path d="M4 4V9H9M20 20V15H15M19.91 14.94C19.4 16.68 18.35 18.19 16.92 19.27C15.49 20.35 13.75 20.94 12 21C7.03 21 3 16.97 3 12M4.09 9.06C4.6 7.32 5.65 5.81 7.08 4.73C8.51 3.65 10.25 3.06 12 3C16.97 3 21 7.03 21 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                  </button>
                </div>
                <div class="viewer-image-wrapper">
                  <div class="viewer-image-container" id="narmail-viewer-image-container">
                    <img
                      src="/images/nar_mail_artwork.webp"
                      alt="Nar-Mail Express Visual Archive"
                      class="viewer-image"
                      id="narmail-viewer-image"
                      draggable="false"
                    />
                  </div>
                </div>
                <div class="viewer-zoom-indicator" id="narmail-zoom-indicator">100%</div>
              </div>
            </div>
            </>
          ) : (
            <article class="panel-article">
              <h2 class="panel-title text-black/80">{zone.title}</h2>
              
              <div class="panel-meta border-black/10">
                {zone.specs.map(spec => (
                  <span class="text-black/40"><strong class="text-[#a67c00]/80 font-medium">{spec.label}</strong>: {spec.value}</span>
                ))}
              </div>
              
              <section class="panel-body text-black/60 font-light">
                <p>{zone.description}</p>
              </section>
              
              <div class="panel-visual border-black/10 bg-black/5">
                <span class="text-black/20">// IMAGE_DATA_REDACTED</span>
              </div>

              <div class="panel-footer border-black/10">
                <button 
                  type="button"
                  data-portal-back 
                  class="panel-back hover:text-[#a67c00]"
                >
                  Retreat to Market
                </button>
              </div>
            </article>
          )}
        </div>
      </div>
    </div>
  ))}
</div>

<script>
  function initPortalNav() {
    ["q-bag", "overpriced-stickers", "afterlife-contracts", "president-of-the-galaxy", "nar-mail-express"].forEach((slug) => {
      const panel = document.querySelector(`[data-portal-panel="${slug}"]`);
      if (!panel) return;

      const nav = panel.querySelector<HTMLElement>("[data-slim-nav]");
      const toggle = nav?.querySelector<HTMLElement>(".slim-toggle");
      const links = nav?.querySelector<HTMLElement>("[data-slim-links]");
      const backButton = panel.querySelector<HTMLButtonElement>("[data-portal-back]");

      if (!nav || !toggle || !links) return;
      if (nav.hasAttribute("data-portal-nav-initialized")) return;
      nav.setAttribute("data-portal-nav-initialized", "true");

      toggle.addEventListener("click", () => {
        const expanded = toggle.getAttribute("aria-expanded") === "true";
        toggle.setAttribute("aria-expanded", String(!expanded));
        links.classList.toggle("open");
      });

      nav.querySelectorAll<HTMLAnchorElement>("a").forEach((link) => {
        link.addEventListener("click", (event) => {
          toggle.setAttribute("aria-expanded", "false");
          links.classList.remove("open");
          const href = link.getAttribute("href");
          if (!href || !href.startsWith("#")) return;
          event.preventDefault();

          if (backButton) backButton.click();

          window.setTimeout(() => {
            window.location.hash = href;
          }, 650);
        });
      });
    });
  }

  if (!document.documentElement.dataset.portalNavListener) {
    document.documentElement.dataset.portalNavListener = "1";
    document.addEventListener("astro:page-load", initPortalNav);
  }
  initPortalNav();

  function initArtworkViewer() {
    const trigger = document.getElementById("artwork-gallery-trigger");
    const viewer = document.getElementById("artwork-viewer");
    const closeBtn = viewer?.querySelector(".viewer-close");
    const zoomInBtn = viewer?.querySelector(".viewer-zoom-in");
    const zoomOutBtn = viewer?.querySelector(".viewer-zoom-out");
    const resetBtn = viewer?.querySelector(".viewer-reset");
    const imageContainer = document.getElementById("viewer-image-container");
    const imageWrapper = viewer?.querySelector(".viewer-image-wrapper");
    const zoomIndicator = document.getElementById("zoom-indicator");

    if (!trigger || !viewer || !imageContainer) return;
    if (viewer.hasAttribute("data-viewer-initialized")) return;
    viewer.setAttribute("data-viewer-initialized", "true");

    let scale = 1;
    let translateX = 0;
    let translateY = 0;
    let isDragging = false;
    let startX = 0;
    let startY = 0;

    const MIN_SCALE = 1;
    const MAX_SCALE = 4;
    const ZOOM_STEP = 0.3;

    function updateTransform() {
      if (!imageContainer) return;
      imageContainer.style.transform = `translate(${translateX}px, ${translateY}px) scale(${scale})`;
      if (zoomIndicator) {
        zoomIndicator.textContent = `${Math.round(scale * 100)}%`;
        showZoomIndicator();
      }
    }

    function showZoomIndicator() {
      if (!zoomIndicator) return;
      zoomIndicator.classList.add("show");
      clearTimeout((window as unknown as { zoomIndicatorTimeout?: number }).zoomIndicatorTimeout);
      (window as unknown as { zoomIndicatorTimeout?: number }).zoomIndicatorTimeout = window.setTimeout(() => {
        zoomIndicator.classList.remove("show");
      }, 1000);
    }

    function openViewer() {
      if (!viewer) return;
      viewer.classList.add("is-open");
      viewer.setAttribute("aria-hidden", "false");
      document.body.style.overflow = "hidden";
      resetView();
    }

    function closeViewer() {
      if (!viewer) return;
      viewer.classList.remove("is-open");
      viewer.setAttribute("aria-hidden", "true");
      document.body.style.overflow = "";
      resetView();
    }

    function resetView() {
      scale = 1;
      translateX = 0;
      translateY = 0;
      updateTransform();
    }

    function zoomIn() {
      if (scale < MAX_SCALE) {
        scale = Math.min(scale + ZOOM_STEP, MAX_SCALE);
        updateTransform();
      }
    }

    function zoomOut() {
      if (scale > MIN_SCALE) {
        scale = Math.max(scale - ZOOM_STEP, MIN_SCALE);
        if (scale === MIN_SCALE) {
          translateX = 0;
          translateY = 0;
        }
        updateTransform();
      }
    }

    function handleWheel(e: WheelEvent) {
      e.preventDefault();
      if (e.deltaY < 0) zoomIn();
      else zoomOut();
    }

    function handleDragStart(e: MouseEvent | TouchEvent) {
      if (scale === 1) return;
      isDragging = true;
      imageWrapper?.classList.add("is-grabbing");
      const clientX = e.type === "mousedown" ? (e as MouseEvent).clientX : (e as TouchEvent).touches[0].clientX;
      const clientY = e.type === "mousedown" ? (e as MouseEvent).clientY : (e as TouchEvent).touches[0].clientY;
      startX = clientX - translateX;
      startY = clientY - translateY;
      if (e.type === "mousedown") {
        document.addEventListener("mousemove", handleDragMove as EventListener);
        document.addEventListener("mouseup", handleDragEnd);
      }
    }

    function handleDragMove(e: MouseEvent | TouchEvent) {
      if (!isDragging) return;
      e.preventDefault();
      const clientX = e.type === "mousemove" ? (e as MouseEvent).clientX : (e as TouchEvent).touches[0].clientX;
      const clientY = e.type === "mousemove" ? (e as MouseEvent).clientY : (e as TouchEvent).touches[0].clientY;
      translateX = clientX - startX;
      translateY = clientY - startY;
      updateTransform();
    }

    function handleDragEnd() {
      isDragging = false;
      imageWrapper?.classList.remove("is-grabbing");
      document.removeEventListener("mousemove", handleDragMove as EventListener);
      document.removeEventListener("mouseup", handleDragEnd);
    }

    let initialDistance = 0;
    let initialScale = 1;

    function getTouchDistance(e: TouchEvent) {
      const touch1 = e.touches[0];
      const touch2 = e.touches[1];
      return Math.hypot(touch2.clientX - touch1.clientX, touch2.clientY - touch1.clientY);
    }

    function handleTouchStart(e: TouchEvent) {
      if (e.touches.length === 2) {
        initialDistance = getTouchDistance(e);
        initialScale = scale;
      } else if (e.touches.length === 1) {
        handleDragStart(e);
      }
    }

    function handleTouchMove(e: TouchEvent) {
      if (e.touches.length === 2) {
        e.preventDefault();
        const currentDistance = getTouchDistance(e);
        const newScale = initialScale * (currentDistance / initialDistance);
        scale = Math.max(MIN_SCALE, Math.min(MAX_SCALE, newScale));
        updateTransform();
      } else if (e.touches.length === 1 && isDragging) {
        handleDragMove(e);
      }
    }

    trigger.addEventListener("click", openViewer);
    closeBtn?.addEventListener("click", closeViewer);
    zoomInBtn?.addEventListener("click", zoomIn);
    zoomOutBtn?.addEventListener("click", zoomOut);
    resetBtn?.addEventListener("click", resetView);

    viewer.querySelector(".viewer-overlay")?.addEventListener("click", closeViewer);

    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && viewer.classList.contains("is-open")) closeViewer();
    });

    imageWrapper?.addEventListener("mousedown", handleDragStart as EventListener);

    imageWrapper?.addEventListener("touchstart", handleTouchStart as EventListener, { passive: false });
    imageWrapper?.addEventListener("touchmove", handleTouchMove as EventListener, { passive: false });
    imageWrapper?.addEventListener("touchend", handleDragEnd);

    imageWrapper?.addEventListener("wheel", handleWheel as EventListener, { passive: false });
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initArtworkViewer);
  } else {
    initArtworkViewer();
  }
  document.addEventListener("astro:page-load", initArtworkViewer);

  function initAfterlifeViewer() {
    const trigger = document.getElementById("afterlife-gallery-trigger");
    const viewer = document.getElementById("afterlife-artwork-viewer");
    const closeBtn = viewer?.querySelector(".viewer-close");
    const zoomInBtn = viewer?.querySelector(".viewer-zoom-in");
    const zoomOutBtn = viewer?.querySelector(".viewer-zoom-out");
    const resetBtn = viewer?.querySelector(".viewer-reset");
    const imageContainer = document.getElementById("afterlife-viewer-image-container");
    const imageWrapper = viewer?.querySelector(".viewer-image-wrapper");
    const zoomIndicator = document.getElementById("afterlife-zoom-indicator");

    if (!trigger || !viewer || !imageContainer) return;
    if (viewer.hasAttribute("data-viewer-initialized")) return;
    viewer.setAttribute("data-viewer-initialized", "true");

    let scale = 1;
    let translateX = 0;
    let translateY = 0;
    let isDragging = false;
    let startX = 0;
    let startY = 0;

    const MIN_SCALE = 1;
    const MAX_SCALE = 4;
    const ZOOM_STEP = 0.3;

    function updateTransform() {
      if (!imageContainer) return;
      imageContainer.style.transform = `translate(${translateX}px, ${translateY}px) scale(${scale})`;
      if (zoomIndicator) {
        zoomIndicator.textContent = `${Math.round(scale * 100)}%`;
        zoomIndicator.classList.add("show");
        clearTimeout((window as { afterlifeZoomTimeout?: number }).afterlifeZoomTimeout);
        (window as { afterlifeZoomTimeout?: number }).afterlifeZoomTimeout = window.setTimeout(() => {
          zoomIndicator.classList.remove("show");
        }, 1000);
      }
    }

    function openViewer() {
      if (!viewer) return;
      viewer.classList.add("is-open");
      viewer.setAttribute("aria-hidden", "false");
      document.body.style.overflow = "hidden";
      resetView();
    }

    function closeViewer() {
      if (!viewer) return;
      viewer.classList.remove("is-open");
      viewer.setAttribute("aria-hidden", "true");
      document.body.style.overflow = "";
      resetView();
    }

    function resetView() {
      scale = 1;
      translateX = 0;
      translateY = 0;
      updateTransform();
    }

    function zoomIn() {
      if (scale < MAX_SCALE) {
        scale = Math.min(scale + ZOOM_STEP, MAX_SCALE);
        updateTransform();
      }
    }

    function zoomOut() {
      if (scale > MIN_SCALE) {
        scale = Math.max(scale - ZOOM_STEP, MIN_SCALE);
        if (scale === MIN_SCALE) {
          translateX = 0;
          translateY = 0;
        }
        updateTransform();
      }
    }

    function handleWheel(e: WheelEvent) {
      e.preventDefault();
      if (e.deltaY < 0) zoomIn();
      else zoomOut();
    }

    function handleDragStart(e: MouseEvent | TouchEvent) {
      if (scale === 1) return;
      isDragging = true;
      imageWrapper?.classList.add("is-grabbing");
      startX = (e.type === "mousedown" ? (e as MouseEvent).clientX : (e as TouchEvent).touches[0].clientX) - translateX;
      startY = (e.type === "mousedown" ? (e as MouseEvent).clientY : (e as TouchEvent).touches[0].clientY) - translateY;
      if (e.type === "mousedown") {
        document.addEventListener("mousemove", handleDragMove as EventListener);
        document.addEventListener("mouseup", handleDragEnd);
      }
    }

    function handleDragMove(e: MouseEvent | TouchEvent) {
      if (!isDragging) return;
      e.preventDefault();
      const clientX = e.type === "mousemove" ? (e as MouseEvent).clientX : (e as TouchEvent).touches[0].clientX;
      const clientY = e.type === "mousemove" ? (e as MouseEvent).clientY : (e as TouchEvent).touches[0].clientY;
      translateX = clientX - startX;
      translateY = clientY - startY;
      updateTransform();
    }

    function handleDragEnd() {
      isDragging = false;
      imageWrapper?.classList.remove("is-grabbing");
      document.removeEventListener("mousemove", handleDragMove as EventListener);
      document.removeEventListener("mouseup", handleDragEnd);
    }

    let initialDistance = 0;
    let initialScale = 1;

    function getTouchDistance(e: TouchEvent) {
      const touch1 = e.touches[0];
      const touch2 = e.touches[1];
      return Math.hypot(touch2.clientX - touch1.clientX, touch2.clientY - touch1.clientY);
    }

    function handleTouchStart(e: TouchEvent) {
      if (e.touches.length === 2) {
        initialDistance = getTouchDistance(e);
        initialScale = scale;
      } else if (e.touches.length === 1) {
        handleDragStart(e);
      }
    }

    function handleTouchMove(e: TouchEvent) {
      if (e.touches.length === 2) {
        e.preventDefault();
        const currentDistance = getTouchDistance(e);
        const newScale = initialScale * (currentDistance / initialDistance);
        scale = Math.max(MIN_SCALE, Math.min(MAX_SCALE, newScale));
        updateTransform();
      } else if (e.touches.length === 1 && isDragging) {
        handleDragMove(e);
      }
    }

    trigger.addEventListener("click", openViewer);
    closeBtn?.addEventListener("click", closeViewer);
    zoomInBtn?.addEventListener("click", zoomIn);
    zoomOutBtn?.addEventListener("click", zoomOut);
    resetBtn?.addEventListener("click", resetView);
    viewer.querySelector(".viewer-overlay")?.addEventListener("click", closeViewer);
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && viewer.classList.contains("is-open")) closeViewer();
    });
    imageWrapper?.addEventListener("mousedown", handleDragStart as EventListener);
    imageWrapper?.addEventListener("touchstart", handleTouchStart as EventListener, { passive: false });
    imageWrapper?.addEventListener("touchmove", handleTouchMove as EventListener, { passive: false });
    imageWrapper?.addEventListener("touchend", handleDragEnd);
    imageWrapper?.addEventListener("wheel", handleWheel as EventListener, { passive: false });
  }

  function initAfterlifeDownload() {
    const checkbox = document.getElementById("afterlife-terms-checkbox");
    const downloadBtn = document.getElementById("afterlife-download-btn");
    const hint = document.getElementById("download-hint");

    if (!checkbox || !downloadBtn) return;

    checkbox.addEventListener("change", () => {
      if ((checkbox as HTMLInputElement).checked) {
        downloadBtn.setAttribute("aria-disabled", "false");
        hint?.classList.add("hidden");
      } else {
        downloadBtn.setAttribute("aria-disabled", "true");
        hint?.classList.remove("hidden");
      }
    });
  }

  function initNarmailViewer() {
    const trigger = document.getElementById("narmail-gallery-trigger");
    const viewer = document.getElementById("narmail-artwork-viewer");
    const closeBtn = viewer?.querySelector(".viewer-close");
    const zoomInBtn = viewer?.querySelector(".viewer-zoom-in");
    const zoomOutBtn = viewer?.querySelector(".viewer-zoom-out");
    const resetBtn = viewer?.querySelector(".viewer-reset");
    const imageContainer = document.getElementById("narmail-viewer-image-container");
    const imageWrapper = viewer?.querySelector(".viewer-image-wrapper");
    const zoomIndicator = document.getElementById("narmail-zoom-indicator");

    if (!trigger || !viewer || !imageContainer) return;
    if (viewer.hasAttribute("data-viewer-initialized")) return;
    viewer.setAttribute("data-viewer-initialized", "true");

    let scale = 1;
    let translateX = 0;
    let translateY = 0;
    let isDragging = false;
    let startX = 0;
    let startY = 0;

    const MIN_SCALE = 1;
    const MAX_SCALE = 4;
    const ZOOM_STEP = 0.3;

    function updateTransform() {
      if (!imageContainer) return;
      imageContainer.style.transform = `translate(${translateX}px, ${translateY}px) scale(${scale})`;
      if (zoomIndicator) {
        zoomIndicator.textContent = `${Math.round(scale * 100)}%`;
        zoomIndicator.classList.add("show");
        clearTimeout((window as { narmailZoomTimeout?: number }).narmailZoomTimeout);
        (window as { narmailZoomTimeout?: number }).narmailZoomTimeout = window.setTimeout(() => {
          zoomIndicator.classList.remove("show");
        }, 1000);
      }
    }

    function openViewer() {
      if (!viewer) return;
      viewer.classList.add("is-open");
      viewer.setAttribute("aria-hidden", "false");
      document.body.style.overflow = "hidden";
      resetView();
    }

    function closeViewer() {
      if (!viewer) return;
      viewer.classList.remove("is-open");
      viewer.setAttribute("aria-hidden", "true");
      document.body.style.overflow = "";
      resetView();
    }

    function resetView() {
      scale = 1;
      translateX = 0;
      translateY = 0;
      updateTransform();
    }

    function zoomIn() {
      if (scale < MAX_SCALE) {
        scale = Math.min(scale + ZOOM_STEP, MAX_SCALE);
        updateTransform();
      }
    }

    function zoomOut() {
      if (scale > MIN_SCALE) {
        scale = Math.max(scale - ZOOM_STEP, MIN_SCALE);
        if (scale === MIN_SCALE) {
          translateX = 0;
          translateY = 0;
        }
        updateTransform();
      }
    }

    function handleWheel(e: WheelEvent) {
      e.preventDefault();
      if (e.deltaY < 0) zoomIn();
      else zoomOut();
    }

    function handleDragStart(e: MouseEvent | TouchEvent) {
      if (scale === 1) return;
      isDragging = true;
      imageWrapper?.classList.add("is-grabbing");
      startX = (e.type === "mousedown" ? (e as MouseEvent).clientX : (e as TouchEvent).touches[0].clientX) - translateX;
      startY = (e.type === "mousedown" ? (e as MouseEvent).clientY : (e as TouchEvent).touches[0].clientY) - translateY;
      if (e.type === "mousedown") {
        document.addEventListener("mousemove", handleDragMove as EventListener);
        document.addEventListener("mouseup", handleDragEnd);
      }
    }

    function handleDragMove(e: MouseEvent | TouchEvent) {
      if (!isDragging) return;
      e.preventDefault();
      const clientX = e.type === "mousemove" ? (e as MouseEvent).clientX : (e as TouchEvent).touches[0].clientX;
      const clientY = e.type === "mousemove" ? (e as MouseEvent).clientY : (e as TouchEvent).touches[0].clientY;
      translateX = clientX - startX;
      translateY = clientY - startY;
      updateTransform();
    }

    function handleDragEnd() {
      isDragging = false;
      imageWrapper?.classList.remove("is-grabbing");
      document.removeEventListener("mousemove", handleDragMove as EventListener);
      document.removeEventListener("mouseup", handleDragEnd);
    }

    let initialDistance = 0;
    let initialScale = 1;

    function getTouchDistance(e: TouchEvent) {
      const touch1 = e.touches[0];
      const touch2 = e.touches[1];
      return Math.hypot(touch2.clientX - touch1.clientX, touch2.clientY - touch1.clientY);
    }

    function handleTouchStart(e: TouchEvent) {
      if (e.touches.length === 2) {
        initialDistance = getTouchDistance(e);
        initialScale = scale;
      } else if (e.touches.length === 1) {
        handleDragStart(e);
      }
    }

    function handleTouchMove(e: TouchEvent) {
      if (e.touches.length === 2) {
        e.preventDefault();
        const currentDistance = getTouchDistance(e);
        const newScale = initialScale * (currentDistance / initialDistance);
        scale = Math.max(MIN_SCALE, Math.min(MAX_SCALE, newScale));
        updateTransform();
      } else if (e.touches.length === 1 && isDragging) {
        handleDragMove(e);
      }
    }

    trigger.addEventListener("click", openViewer);
    closeBtn?.addEventListener("click", closeViewer);
    zoomInBtn?.addEventListener("click", zoomIn);
    zoomOutBtn?.addEventListener("click", zoomOut);
    resetBtn?.addEventListener("click", resetView);
    viewer.querySelector(".viewer-overlay")?.addEventListener("click", closeViewer);
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && viewer.classList.contains("is-open")) closeViewer();
    });
    imageWrapper?.addEventListener("mousedown", handleDragStart as EventListener);
    imageWrapper?.addEventListener("touchstart", handleTouchStart as EventListener, { passive: false });
    imageWrapper?.addEventListener("touchmove", handleTouchMove as EventListener, { passive: false });
    imageWrapper?.addEventListener("touchend", handleDragEnd);
    imageWrapper?.addEventListener("wheel", handleWheel as EventListener, { passive: false });
  }

  function initPresidentViewer() {
    const trigger = document.getElementById("president-gallery-trigger");
    const viewer = document.getElementById("president-artwork-viewer");
    const closeBtn = viewer?.querySelector(".viewer-close");
    const zoomInBtn = viewer?.querySelector(".viewer-zoom-in");
    const zoomOutBtn = viewer?.querySelector(".viewer-zoom-out");
    const resetBtn = viewer?.querySelector(".viewer-reset");
    const imageContainer = document.getElementById("president-viewer-image-container");
    const imageWrapper = viewer?.querySelector(".viewer-image-wrapper");
    const zoomIndicator = document.getElementById("president-zoom-indicator");

    if (!trigger || !viewer || !imageContainer) return;
    if (viewer.hasAttribute("data-viewer-initialized")) return;
    viewer.setAttribute("data-viewer-initialized", "true");

    let scale = 1;
    let translateX = 0;
    let translateY = 0;
    let isDragging = false;
    let startX = 0;
    let startY = 0;

    const MIN_SCALE = 1;
    const MAX_SCALE = 4;
    const ZOOM_STEP = 0.3;

    function updateTransform() {
      if (!imageContainer) return;
      imageContainer.style.transform = `translate(${translateX}px, ${translateY}px) scale(${scale})`;
      if (zoomIndicator) {
        zoomIndicator.textContent = `${Math.round(scale * 100)}%`;
        zoomIndicator.classList.add("show");
        clearTimeout((window as { presidentZoomTimeout?: number }).presidentZoomTimeout);
        (window as { presidentZoomTimeout?: number }).presidentZoomTimeout = window.setTimeout(() => {
          zoomIndicator.classList.remove("show");
        }, 1000);
      }
    }

    function openViewer() {
      if (!viewer) return;
      viewer.classList.add("is-open");
      viewer.setAttribute("aria-hidden", "false");
      document.body.style.overflow = "hidden";
      resetView();
    }

    function closeViewer() {
      if (!viewer) return;
      viewer.classList.remove("is-open");
      viewer.setAttribute("aria-hidden", "true");
      document.body.style.overflow = "";
      resetView();
    }

    function resetView() {
      scale = 1;
      translateX = 0;
      translateY = 0;
      updateTransform();
    }

    function zoomIn() {
      if (scale < MAX_SCALE) {
        scale = Math.min(scale + ZOOM_STEP, MAX_SCALE);
        updateTransform();
      }
    }

    function zoomOut() {
      if (scale > MIN_SCALE) {
        scale = Math.max(scale - ZOOM_STEP, MIN_SCALE);
        if (scale === MIN_SCALE) {
          translateX = 0;
          translateY = 0;
        }
        updateTransform();
      }
    }

    function handleWheel(e: WheelEvent) {
      e.preventDefault();
      if (e.deltaY < 0) zoomIn();
      else zoomOut();
    }

    function handleDragStart(e: MouseEvent | TouchEvent) {
      if (scale === 1) return;
      isDragging = true;
      imageWrapper?.classList.add("is-grabbing");
      startX = (e.type === "mousedown" ? (e as MouseEvent).clientX : (e as TouchEvent).touches[0].clientX) - translateX;
      startY = (e.type === "mousedown" ? (e as MouseEvent).clientY : (e as TouchEvent).touches[0].clientY) - translateY;
      if (e.type === "mousedown") {
        document.addEventListener("mousemove", handleDragMove as EventListener);
        document.addEventListener("mouseup", handleDragEnd);
      }
    }

    function handleDragMove(e: MouseEvent | TouchEvent) {
      if (!isDragging) return;
      e.preventDefault();
      const clientX = e.type === "mousemove" ? (e as MouseEvent).clientX : (e as TouchEvent).touches[0].clientX;
      const clientY = e.type === "mousemove" ? (e as MouseEvent).clientY : (e as TouchEvent).touches[0].clientY;
      translateX = clientX - startX;
      translateY = clientY - startY;
      updateTransform();
    }

    function handleDragEnd() {
      isDragging = false;
      imageWrapper?.classList.remove("is-grabbing");
      document.removeEventListener("mousemove", handleDragMove as EventListener);
      document.removeEventListener("mouseup", handleDragEnd);
    }

    let initialDistance = 0;
    let initialScale = 1;

    function getTouchDistance(e: TouchEvent) {
      const touch1 = e.touches[0];
      const touch2 = e.touches[1];
      return Math.hypot(touch2.clientX - touch1.clientX, touch2.clientY - touch1.clientY);
    }

    function handleTouchStart(e: TouchEvent) {
      if (e.touches.length === 2) {
        initialDistance = getTouchDistance(e);
        initialScale = scale;
      } else if (e.touches.length === 1) {
        handleDragStart(e);
      }
    }

    function handleTouchMove(e: TouchEvent) {
      if (e.touches.length === 2) {
        e.preventDefault();
        const currentDistance = getTouchDistance(e);
        const newScale = initialScale * (currentDistance / initialDistance);
        scale = Math.max(MIN_SCALE, Math.min(MAX_SCALE, newScale));
        updateTransform();
      } else if (e.touches.length === 1 && isDragging) {
        handleDragMove(e);
      }
    }

    trigger.addEventListener("click", openViewer);
    closeBtn?.addEventListener("click", closeViewer);
    zoomInBtn?.addEventListener("click", zoomIn);
    zoomOutBtn?.addEventListener("click", zoomOut);
    resetBtn?.addEventListener("click", resetView);

    viewer.querySelector(".viewer-overlay")?.addEventListener("click", closeViewer);

    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && viewer.classList.contains("is-open")) closeViewer();
    });

    imageWrapper?.addEventListener("mousedown", handleDragStart as EventListener);

    imageWrapper?.addEventListener("touchstart", handleTouchStart as EventListener, { passive: false });
    imageWrapper?.addEventListener("touchmove", handleTouchMove as EventListener, { passive: false });
    imageWrapper?.addEventListener("touchend", handleDragEnd);

    imageWrapper?.addEventListener("wheel", handleWheel as EventListener, { passive: false });
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", () => {
      initAfterlifeViewer();
      initAfterlifeDownload();
      initPresidentViewer();
      initNarmailViewer();
    });
  } else {
    initAfterlifeViewer();
    initAfterlifeDownload();
    initPresidentViewer();
    initNarmailViewer();
  }
  document.addEventListener("astro:page-load", () => {
    initAfterlifeViewer();
    initAfterlifeDownload();
    initPresidentViewer();
    initNarmailViewer();
  });
</script>

<style>
  .infocigan-portal-system {
    position: fixed;
    inset: 0;
    z-index: 200;
    pointer-events: none;
  }

  .portal-panel {
    position: absolute;
    inset: 0;
    pointer-events: none;
    visibility: hidden;
  }

  .portal-panel.is-active {
    pointer-events: auto;
    visibility: visible;
  }

  .panel-overlay {
    position: fixed;
    inset: 0;
    background: #343d46/40;
    backdrop-filter: blur(12px);
    opacity: 0;
  }

  .panel-content {
    position: absolute;
    inset: 0;
    background: #65737e;
    opacity: 0;
    display: flex;
    flex-direction: column;
  }

  [data-portal-panel="q-bag"] .panel-content {
    background: #0a0a0a;
  }

  /* Overpriced Stickers: Match Infocigan fog */
  [data-portal-panel="overpriced-stickers"] .panel-content {
    background: var(--bg-fog);
  }

  [data-portal-panel="afterlife-contracts"] .panel-content {
    background: #fcf8f9;
  }

  [data-portal-panel="president-of-the-galaxy"] .panel-content {
    background: transparent;
  }

  [data-portal-panel="nar-mail-express"] .panel-content {
    background: #fcf8f9;
  }

  [data-portal-panel="nar-mail-express"] #nar-chat-widget .nar-chat-message-content,
  [data-portal-panel="nar-mail-express"] #nar-chat-widget .nar-chat-header-content h3,
  [data-portal-panel="nar-mail-express"] #nar-chat-widget .nar-chat-status,
  [data-portal-panel="nar-mail-express"] #nar-chat-widget .nar-chat-input,
  [data-portal-panel="nar-mail-express"] #nar-chat-widget .nar-chat-close {
    color: #ffffff !important;
  }

  .panel-scroll {
    flex: 1;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
  }

  .panel-article {
    max-width: 48rem;
    margin: 0 auto;
    padding: 6rem 1.5rem 8rem;
    min-height: 100%;
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 768px) {
    .panel-article {
      padding: 8rem 6rem 8rem;
    }
  }

  .panel-footer {
    display: flex;
    justify-content: center;
    align-items: center;
    padding-top: 4rem;
    margin-top: auto;
    padding-bottom: 4rem;
    border-top: 1px solid rgba(0,0,0,0.1);
  }

  .panel-back {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.65rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(0,0,0,0.4);
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    transition: all 300ms ease;
  }

  .panel-title {
    font-size: clamp(2rem, 6vw, 3.5rem);
    font-weight: 300;
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
    text-transform: uppercase;
  }

  .panel-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem 2rem;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.625rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 4rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(0,0,0,0.1);
  }

  .panel-body {
    font-size: 1rem;
    line-height: 1.8;
    max-width: 45ch;
    margin-bottom: 3rem;
  }

  @media (min-width: 768px) {
    .panel-body {
      font-size: 1.125rem;
    }
  }

  .panel-visual {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.625rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    padding: 6rem 2rem;
    border: 1px solid rgba(0,0,0,0.1);
    text-align: center;
  }

  [data-portal-panel="q-bag"] .panel-archive-bar,
  [data-portal-panel="overpriced-stickers"] .panel-archive-bar {
    display: none;
  }

  .overpriced-layout-with-nav {
    --overpriced-nav-offset: 2.5rem;
    padding-top: var(--overpriced-nav-offset);
  }

  @media (min-width: 768px) {
    .overpriced-layout-with-nav {
      --overpriced-nav-offset: 2.75rem;
    }
  }

  /* Q-Bag layout - clean & modern */
  .qbag-new-layout {
    width: 100%;
    background: #0a0a0a;
    color: var(--text-primary, #e0e0e0);
    font-family: inherit;
    overflow-y: auto;
  }

  .qbag-hero {
    width: 100%;
    height: 80vh;
    position: relative;
    overflow: hidden;
  }

  .qbag-hero img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .qbag-hero-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 4vw;
    background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
  }

  .qbag-title {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(5rem, 14vw, 14rem);
    font-weight: 900;
    line-height: 0.85;
    text-transform: uppercase;
    color: var(--accent-primary, #ffd700);
    margin: 0;
    letter-spacing: -0.04em;
    text-shadow: 0 4px 20px rgba(255, 215, 0, 0.3);
  }

  .qbag-main-content {
    max-width: 1600px;
    margin: 0 auto;
    padding: 0 4vw;
  }

  .qbag-intro-section {
    padding: 8rem 0 6rem;
  }

  .intro-card {
    max-width: 1200px;
    margin: 0 auto;
    padding: 5rem 4rem;
    background: rgba(10, 10, 10, 0.85);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  }

  .qbag-tagline {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    font-weight: 600;
    color: var(--accent-primary, #ffd700);
    margin-bottom: 2rem;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    line-height: 1;
    opacity: 0.95;
  }

  .qbag-lead {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.5rem, 2.5vw, 2.2rem);
    line-height: 1.4;
    font-weight: 300;
    opacity: 0.95;
    margin: 0;
    letter-spacing: -0.01em;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }

  .qbag-artwork-card-section {
    padding: 6rem 0;
  }

  .qbag-artwork-card-section .artwork-info-card {
    background-image: linear-gradient(rgba(10, 10, 10, 0.78), rgba(10, 10, 10, 0.78)), url("/images/qbag_concept.webp");
    background-size: cover;
    background-position: center;
  }

  .artwork-info-card {
    max-width: 1000px;
    margin: 0 auto;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .artwork-info-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 215, 0, 0.1), transparent);
    transition: left 0.5s ease;
  }

  .artwork-info-card:hover::before {
    left: 100%;
  }

  .artwork-info-card:hover {
    border-color: rgba(255, 215, 0, 0.5);
    transform: translateY(-4px);
    box-shadow: 0 12px 48px rgba(255, 215, 0, 0.2);
  }

  .artwork-card-content {
    display: flex;
    align-items: center;
    gap: 2rem;
    padding: 3rem;
  }

  .artwork-card-icon {
    flex-shrink: 0;
    width: 64px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--accent-primary, #ffd700);
    background: rgba(255, 215, 0, 0.1);
    border-radius: 12px;
  }

  .artwork-card-text {
    flex: 1;
  }

  .artwork-card-title {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.75rem, 3vw, 2.5rem);
    font-weight: 700;
    color: var(--accent-primary, #ffd700);
    margin: 0 0 1rem 0;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }

  .artwork-card-title::before {
    content: '// GALLERY';
    display: block;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.65rem;
    font-weight: 500;
    color: rgba(255, 215, 0, 0.5);
    letter-spacing: 0.2em;
    margin-bottom: 0.75rem;
    text-transform: uppercase;
  }

  .artwork-card-description {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.05rem, 1.3vw, 1.3rem);
    line-height: 1.7;
    opacity: 0.85;
    margin: 0;
    font-weight: 300;
    letter-spacing: -0.005em;
  }

  .artwork-card-button {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    background: var(--accent-primary, #ffd700);
    color: #000;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .artwork-card-button:hover {
    transform: scale(1.05);
    box-shadow: 0 4px 16px rgba(255, 215, 0, 0.4);
  }

  .artwork-card-button svg {
    transition: transform 0.2s ease;
  }

  .artwork-card-button:hover svg {
    transform: translateX(4px);
  }

  @media (max-width: 768px) {
    .artwork-card-content {
      flex-direction: column;
      text-align: center;
      padding: 2rem;
      gap: 1.5rem;
    }
    .artwork-card-button {
      width: 100%;
      justify-content: center;
    }
  }

  .artwork-viewer {
    position: fixed;
    inset: 0;
    z-index: 10000;
    display: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .artwork-viewer.is-open {
    display: block;
    opacity: 1;
  }

  .viewer-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.98);
    backdrop-filter: blur(10px);
  }

  .viewer-container {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .viewer-controls {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    display: flex;
    gap: 0.5rem;
    z-index: 10002;
  }

  .viewer-btn {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    color: #fff;
    cursor: pointer;
    transition: all 0.2s ease;
    backdrop-filter: blur(10px);
  }

  .viewer-btn:hover {
    background: rgba(255, 215, 0, 0.2);
    border-color: var(--accent-primary, #ffd700);
    transform: scale(1.05);
  }

  .viewer-close {
    background: rgba(255, 50, 50, 0.15);
    border-color: rgba(255, 50, 50, 0.3);
  }

  .viewer-close:hover {
    background: rgba(255, 50, 50, 0.3);
    border-color: rgba(255, 50, 50, 0.6);
  }

  .viewer-image-wrapper {
    width: 100%;
    height: 100%;
    overflow: hidden;
    position: relative;
    cursor: grab;
  }

  .viewer-image-wrapper.is-grabbing {
    cursor: grabbing;
  }

  .viewer-image-container {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    transform-origin: center center;
    transition: transform 0.2s ease;
    will-change: transform;
  }

  .viewer-image {
    max-width: 90vw;
    max-height: 90vh;
    width: auto;
    height: auto;
    object-fit: contain;
    user-select: none;
    pointer-events: none;
  }

  .viewer-zoom-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    padding: 0.5rem 1rem;
    background: rgba(0, 0, 0, 0.7);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 20px;
    color: #fff;
    font-size: 0.875rem;
    font-weight: 600;
    opacity: 0;
    transition: opacity 0.2s ease;
    pointer-events: none;
    z-index: 10002;
  }

  .viewer-zoom-indicator.show {
    opacity: 1;
  }

  @media (max-width: 768px) {
    .viewer-controls {
      top: 1rem;
      right: 1rem;
      gap: 0.375rem;
    }
    .viewer-btn {
      width: 40px;
      height: 40px;
    }
    .viewer-image {
      max-width: 95vw;
      max-height: 95vh;
    }
  }

  .qbag-content-grid-section {
    padding: 6rem 0;
  }

  .qbag-glass-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 3rem;
  }

  .glass-card {
    background: rgba(10, 10, 10, 0.7);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 3.5rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
    transition: all 0.3s ease;
  }

  .glass-card:hover {
    background: rgba(15, 15, 15, 0.75);
    border-color: rgba(255, 215, 0, 0.3);
    transform: translateY(-4px);
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.6);
  }

  .glass-card h3 {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.8rem, 2.5vw, 2.8rem);
    font-weight: 700;
    margin-bottom: 2rem;
    color: var(--accent-primary, #ffd700);
    border-bottom: none;
    padding-bottom: 0;
    display: block;
    line-height: 1.1;
    letter-spacing: -0.02em;
    text-shadow: 0 0 40px rgba(255, 215, 0, 0.15);
  }

  .glass-card h3::before {
    content: '//';
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.7em;
    color: rgba(255, 215, 0, 0.4);
    margin-right: 0.75rem;
    font-weight: 400;
  }

  .glass-card p {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.05rem, 1.3vw, 1.3rem);
    line-height: 1.75;
    opacity: 0.9;
    margin-bottom: 1.75rem;
    font-weight: 400;
    letter-spacing: -0.005em;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
    animation: fadeInUp 0.6s ease-out backwards;
    animation-delay: calc(var(--delay, 0) * 0.1s);
  }

  .glass-card p:first-of-type {
    font-size: clamp(1.15rem, 1.4vw, 1.4rem);
    line-height: 1.7;
    font-weight: 300;
  }

  .glass-card p:last-child {
    margin-bottom: 0;
  }

  .qbag-partners-section {
    padding: 8rem 0;
  }

  .qbag-partners-section h2 {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(2.5rem, 4.5vw, 4.5rem);
    font-weight: 800;
    margin-bottom: 3rem;
    color: var(--accent-primary, #ffd700);
    line-height: 1;
    letter-spacing: -0.03em;
    text-shadow: 0 0 50px rgba(255, 215, 0, 0.2);
  }

  .partners-card h2::before {
    content: '// FOR PARTNERS';
    display: block;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.75rem;
    font-weight: 500;
    color: rgba(255, 215, 0, 0.5);
    letter-spacing: 0.2em;
    margin-bottom: 1rem;
    text-transform: uppercase;
  }

  .qbag-partners-section p {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.15rem, 1.6vw, 1.6rem);
    line-height: 1.7;
    margin-bottom: 2rem;
    opacity: 0.9;
    font-weight: 300;
    letter-spacing: -0.005em;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }

  .partners-card {
    max-width: 1200px;
    margin: 0 auto;
    padding: 5rem 4rem;
  }

  .qbag-closing-section {
    padding: 8rem 0;
    text-align: center;
  }

  .closing-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 6rem 3rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .big-statement {
    font-family: Georgia, "Times New Roman", Times, serif;
    font-size: clamp(2.5rem, 6vw, 5.5rem);
    font-weight: 400;
    font-style: italic;
    line-height: 1.3;
    color: rgba(255, 255, 255, 0.85);
    margin: 0;
    letter-spacing: -0.02em;
  }

  .qbag-contact-section {
    padding: 6rem 0 8rem;
  }

  .contact-card {
    max-width: 900px;
    margin: 0 auto;
    padding: 4rem 3rem;
    text-align: center;
  }

  .contact-card p {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.05rem, 1.4vw, 1.4rem);
    line-height: 1.7;
    margin-bottom: 1.5rem;
    font-weight: 300;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }

  .qbag-new-layout .contact-info {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace !important;
    font-size: clamp(1rem, 1.3vw, 1.3rem) !important;
    font-weight: 500 !important;
    letter-spacing: 0.02em;
    margin-top: 2rem;
  }

  .contact-card a {
    color: #ce9e00;
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .contact-card a:hover {
    opacity: 0.7;
    text-decoration: underline;
  }

  .qbag-cta {
    padding: 6rem 0;
    text-align: center;
    background: #0a0a0a;
  }

  .qbag-new-layout .retreat-button {
    background: transparent;
    border: 2px solid #a67c00;
    color: #ce9e00;
    padding: 1.5rem 4rem;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 1.1rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.3s ease;
    border-radius: 4px;
  }

  .qbag-new-layout .retreat-button:hover {
    background: #ce9e00;
    color: #000;
    box-shadow: 0 0 30px rgba(166, 124, 0, 0.3);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 0.9;
      transform: translateY(0);
    }
  }

  @media (max-width: 1024px) {
    .qbag-glass-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .qbag-title {
      font-size: clamp(4rem, 12vw, 10rem);
    }
    .qbag-tagline {
      letter-spacing: 0.2em;
    }
    .glass-card h3 {
      font-size: clamp(1.6rem, 2.2vw, 2.4rem);
    }
    .intro-card,
    .partners-card,
    .contact-card {
      padding: 3rem 2rem;
    }
  }

  @media (max-width: 768px) {
    .qbag-hero {
      height: 60vh;
    }
    .qbag-main-content {
      padding: 0 3vw;
    }
    .qbag-title {
      font-size: clamp(3rem, 10vw, 8rem);
      letter-spacing: -0.03em;
    }
    .qbag-tagline {
      font-size: 0.9rem;
      letter-spacing: 0.15em;
    }
    .qbag-lead {
      font-size: clamp(1.25rem, 2vw, 1.8rem);
    }
    .qbag-intro-section {
      padding: 5rem 0 4rem;
    }
    .intro-card {
      padding: 3rem 2.5rem;
    }
    .qbag-content-grid-section {
      padding: 3rem 0;
    }
    .qbag-glass-grid {
      gap: 2rem;
    }
    .glass-card {
      padding: 2.5rem 2rem;
    }
    .glass-card h3 {
      font-size: clamp(1.5rem, 2vw, 2rem);
      margin-bottom: 1.5rem;
    }
    .glass-card p {
      font-size: 1.05rem;
      line-height: 1.7;
    }
    .big-statement {
      font-size: clamp(2rem, 5vw, 4rem);
    }
    .qbag-partners-section h2 {
      font-size: clamp(2rem, 4vw, 3.5rem);
    }
    .qbag-partners-section,
    .qbag-closing-section {
      padding: 5rem 0;
    }
    .qbag-contact-section {
      padding: 5rem 0 6rem;
    }
    .partners-card,
    .contact-card {
      padding: 3rem 2rem;
    }
    .closing-content {
      padding: 3rem 1rem;
    }
  }

  @media (max-width: 480px) {
    .qbag-hero {
      height: 50vh;
    }
    .qbag-title {
      font-size: clamp(2.5rem, 12vw, 6rem);
    }
    .qbag-tagline {
      font-size: 0.8rem;
      letter-spacing: 0.1em;
    }
    .intro-card {
      padding: 2rem 1.5rem;
    }
    .glass-card {
      padding: 1.5rem;
    }
    .artwork-card-title {
      font-size: 1.5rem;
    }
    .qbag-new-layout .retreat-button {
      padding: 1rem 2rem;
      font-size: 1rem;
    }
  }

  /* Gold Shine Effect */
  .gold-shine {
    color: #a67c00;
    background: linear-gradient(
      to right, 
      #a67c00 20%, 
      #ce9e00 40%, 
      #ffcc33 50%, 
      #ce9e00 60%, 
      #a67c00 80%
    );
    background-size: 200% auto;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: shine 8s linear infinite;
  }

  @keyframes shine {
    to {
      background-position: 200% center;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .qbag-new-layout .gold-shine {
      animation: none;
    }
    .glass-card p {
      animation: none;
    }
  }

  /* AFTERLIFE CONTRACT - Nar-Mail Express structure & fonts */
  .afterlife-eden-wrap {
    --eden-bg: #fcf8f9;
    --eden-text: #134252;
    --eden-text-muted: #627078;
    --eden-primary: #208c8d;
    position: relative;
    min-height: 100vh;
    background: var(--eden-bg);
  }

  .afterlife-narmail-layout {
    width: 100%;
    color: var(--eden-text);
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    overflow-y: auto;
  }

  .afterlife-hero {
    position: relative;
  }

  .afterlife-hero-image {
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    background: var(--eden-bg);
  }

  .afterlife-hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .afterlife-hero-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    background: linear-gradient(to top, rgba(252, 248, 249, 0.95), transparent);
  }

  .afterlife-headline {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
  }

  .afterlife-mono {
    font-family: ui-monospace, "SF Mono", Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
  }

  .afterlife-mono-gold {
    color: #a67c00;
    background: linear-gradient(to right, #a67c00 20%, #ce9e00 40%, #ffcc33 50%, #ce9e00 60%, #a67c00 80%);
    background-size: 200% auto;
    background-position: 0% center;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: afterlife-gold-shine 8s linear infinite;
  }

  @keyframes afterlife-gold-shine {
    to { background-position: 200% center; }
  }

  @media (prefers-reduced-motion: reduce) {
    .afterlife-mono-gold {
      animation: none;
      background-position: 50% center;
    }
  }

  .afterlife-main-content {
    max-width: 72rem;
    margin: 0 auto;
  }

  .afterlife-section {
    padding: 4rem 0;
    border-bottom: 1px solid rgba(19, 66, 82, 0.12);
  }

  .afterlife-section:last-of-type {
    border-bottom: none;
  }

  .afterlife-h2 {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.8rem, 3.2vw, 3rem);
    font-weight: 500;
    color: var(--eden-text);
    margin: 0 0 1.5rem 0;
    letter-spacing: -0.035em;
    line-height: 0.9;
  }

  .afterlife-h3 {
    font-size: clamp(1.4rem, 2.2vw, 2.2rem);
    font-weight: 500;
    color: var(--eden-text);
    margin: 2.5rem 0 1rem 0;
    letter-spacing: -0.02em;
  }

  .afterlife-section p {
    font-size: clamp(1.4rem, 2.2vw, 2.2rem);
    line-height: 1.4;
    color: var(--eden-text);
    margin: 0 0 1.5rem 0;
  }

  .afterlife-list {
    list-style: none;
    margin: 0 0 1.5rem 0;
    padding: 0;
  }

  .afterlife-list li {
    font-size: clamp(1.4rem, 2.2vw, 2.2rem);
    line-height: 1.4;
    color: var(--eden-text);
    margin-bottom: 0.75rem;
    padding-left: 2.25rem;
    position: relative;
  }

  .afterlife-list li::before {
    content: '✦';
    position: absolute;
    left: 0;
    color: #a67c00;
    background: linear-gradient(to right, #a67c00 20%, #ce9e00 40%, #ffcc33 50%, #ce9e00 60%, #a67c00 80%);
    background-size: 200% auto;
    background-position: 0% center;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: afterlife-gold-shine 8s linear infinite;
  }

  @media (prefers-reduced-motion: reduce) {
    .afterlife-list li::before {
      animation: none;
      background-position: 50% center;
    }
  }

  .afterlife-blockquote {
    font-size: clamp(1.4rem, 2vw, 2rem);
    font-style: italic;
    line-height: 1.5;
    color: var(--eden-text);
    margin: 1.5rem 0;
    padding: 0 0 0 2rem;
    border-left: 4px solid var(--eden-primary);
  }

  .afterlife-terms {
    margin: 0 0 1rem 0;
    padding-left: 1.5rem;
    color: var(--eden-text);
    font-size: clamp(1.2rem, 1.6vw, 1.6rem);
    line-height: 1.6;
  }

  .afterlife-terms h3 {
    margin: 1.5rem 0 1rem 0;
  }

  .terms-checkbox-label {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    cursor: pointer;
    font-size: 1.05rem;
    line-height: 1.6;
    font-weight: 500;
    color: var(--eden-text);
  }

  .terms-checkbox {
    width: 20px;
    height: 20px;
    flex-shrink: 0;
    cursor: pointer;
    accent-color: #a67c00;
  }

  .afterlife-download-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1.25rem 3rem;
    background: transparent;
    color: rgba(19, 66, 82, 0.4);
    border: 2px solid rgba(19, 66, 82, 0.3);
    border-radius: 8px;
    font-family: ui-monospace, "SF Mono", Menlo, Monaco, Consolas, monospace;
    font-size: 0.95rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    text-decoration: none;
    cursor: not-allowed;
    transition: all 0.3s ease;
    pointer-events: none;
  }

  .afterlife-download-btn[aria-disabled="false"] {
    background: #a67c00;
    color: #0a0a0a;
    border-color: #a67c00;
    cursor: pointer;
    pointer-events: auto;
  }

  .afterlife-download-btn[aria-disabled="false"]:hover {
    background: #ce9e00;
    border-color: #ce9e00;
  }

  .download-hint {
    margin-top: 1rem;
    font-size: 0.875rem;
    font-style: italic;
    color: var(--eden-text-muted);
  }

  .download-hint.hidden {
    display: none;
  }

  .afterlife-retreat-btn {
    display: inline-block;
    padding: 1rem 2.5rem;
    background: transparent;
    color: var(--eden-text);
    border: 2px solid var(--eden-text);
    font-family: ui-monospace, "SF Mono", Menlo, Monaco, Consolas, monospace;
    font-size: 0.95rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .afterlife-retreat-btn:hover {
    background: var(--eden-text);
    color: var(--eden-bg);
  }

  .afterlife-cta {
    text-align: center;
  }

  /* Gallery card - same as Nar-Mail narmail-glass */
  .afterlife-artwork-card-section {
    padding: 5rem 0;
  }

  .artwork-info-card.afterlife-narmail-glass {
    background: #0a0a0a;
    border: 1px solid #3a3a3a;
    max-width: 960px;
    margin: 0 auto;
    position: relative;
    overflow: hidden;
  }

  .afterlife-artwork-card-section .artwork-info-card.afterlife-narmail-glass {
    background-image: linear-gradient(rgba(10, 10, 10, 0.92), rgba(10, 10, 10, 0.92)), url("/images/afterlife_contract.webp");
    background-size: cover;
    background-position: center;
  }

  .artwork-info-card.afterlife-narmail-glass::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(206, 158, 0, 0.08), transparent);
    transition: left 0.6s ease;
  }

  .artwork-info-card.afterlife-narmail-glass:hover {
    border-color: rgba(206, 158, 0, 0.5);
    box-shadow: 0 16px 56px rgba(0, 0, 0, 0.5), 0 0 0 1px rgba(206, 158, 0, 0.15);
    transform: translateY(-2px);
  }

  .artwork-info-card.afterlife-narmail-glass:hover::before {
    left: 100%;
  }

  .artwork-info-card.afterlife-narmail-glass .artwork-card-title.afterlife-gallery-title {
    background: linear-gradient(to right, #a67c00 20%, #ce9e00 40%, #ffcc33 50%, #ce9e00 60%, #a67c00 80%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 200% auto;
    background-position: 0% center;
    animation: afterlife-shine 8s linear infinite;
  }

  .artwork-info-card.afterlife-narmail-glass .artwork-card-title.afterlife-gallery-title::before {
    content: '// GALLERY';
    display: block;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.65rem;
    font-weight: 500;
    color: rgba(206, 158, 0, 0.7);
    letter-spacing: 0.2em;
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    -webkit-text-fill-color: initial;
  }

  .artwork-info-card.afterlife-narmail-glass .artwork-card-description {
    color: rgba(255, 255, 255, 0.88);
    font-weight: 400;
  }

  .artwork-info-card.afterlife-narmail-glass .artwork-card-icon.afterlife-gallery-icon {
    background: rgba(206, 158, 0, 0.15);
    color: #ce9e00;
  }

  .artwork-card-button.afterlife-gallery-btn {
    background: transparent;
    color: #ce9e00;
    border: 2px solid #ce9e00;
  }

  .artwork-card-button.afterlife-gallery-btn:hover {
    background: #ce9e00;
    color: #0a0a0a;
    box-shadow: 0 4px 24px rgba(206, 158, 0, 0.35);
  }

  @keyframes afterlife-shine {
    to { background-position: 200% center; }
  }

  @media (prefers-reduced-motion: reduce) {
    .artwork-info-card.afterlife-narmail-glass .artwork-card-title.afterlife-gallery-title {
      animation: none;
      background-position: 50% center;
    }
  }

  @media (max-width: 768px) {
    .afterlife-artwork-card-section {
      padding: 3rem 0;
    }
    .afterlife-artwork-card-section .artwork-card-content {
      flex-direction: column;
      text-align: center;
      padding: 2rem;
    }
    .afterlife-artwork-card-section .artwork-card-button {
      width: 100%;
      justify-content: center;
    }
    .afterlife-section {
      padding: 3rem 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .artwork-info-card.afterlife-narmail-glass:hover,
    .afterlife-retreat-btn {
      transform: none;
      transition: none;
    }
  }

  /* President of the Galaxy - Cosmic Theme */
  .president-content {
    position: relative;
    z-index: 1;
    --deep-space: 7, 19, 35;
    --space-navy: 15, 35, 55;
    --teal-glow: 45, 140, 155;
    --bright-cyan: 95, 210, 230;
    --ice-blue: 180, 235, 245;
    --champagne: 235, 215, 175;
  }

  .president-zone {
    position: relative;
    min-height: 100vh;
    z-index: 1;
  }

  .president-bg-wrap {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .president-layout {
    position: relative;
    z-index: 1;
    width: 100%;
    background: transparent;
    color: rgb(var(--ice-blue));
    font-family: inherit;
    overflow-y: auto;
  }

  .president-layout ::selection {
    background: rgba(var(--bright-cyan), 0.3);
    color: rgb(var(--ice-blue));
  }

  .president-hero {
    width: 100%;
    height: 80vh;
    position: relative;
    overflow: hidden;
    background: rgb(var(--deep-space));
  }

  .president-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: linear-gradient(
      to bottom,
      rgba(7, 19, 35, 0.35) 0%,
      rgba(15, 35, 55, 0.25) 50%,
      rgba(45, 140, 155, 0.15) 100%
    );
  }

  .president-hero img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .president-hero-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 5vw 5vw 2vw 5vw;
    background: transparent;
  }

  .president-title {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(4rem, 11.2vw, 11.2rem);
    font-weight: 900;
    line-height: 0.85;
    text-transform: uppercase;
    margin: 0 0 1rem 0;
    letter-spacing: -0.04em;
    background: linear-gradient(
      135deg,
      rgb(var(--ice-blue)) 0%,
      rgb(var(--bright-cyan)) 25%,
      rgb(200, 165, 105) 50%,
      rgb(var(--bright-cyan)) 75%,
      rgb(var(--ice-blue)) 100%
    );
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 200% auto;
    -webkit-text-stroke: 2px rgb(var(--bright-cyan));
    filter: drop-shadow(0 0 25px rgba(var(--bright-cyan), 0.6)) drop-shadow(0 4px 18px rgba(var(--teal-glow), 0.4));
    animation: metallic-shine 8s linear infinite;
  }

  @keyframes metallic-shine {
    to { background-position: 200% center; }
  }

  .president-main-content {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 4vw;
  }

  .president-intro-section,
  .president-artwork-card-section,
  .president-content-grid-section,
  .president-interaction-section,
  .president-contact-section {
    padding: 8rem 0;
  }

  .president-intro-section::before,
  .president-artwork-card-section::before,
  .president-content-grid-section::before,
  .president-interaction-section::before,
  .president-contact-section::before {
    content: '';
    display: block;
    width: 60px;
    height: 2px;
    background: linear-gradient(
      to right,
      transparent,
      rgb(var(--bright-cyan)),
      transparent
    );
    margin-bottom: 2rem;
  }

  .president-tagline {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.875rem;
    font-weight: 600;
    color: rgb(var(--champagne));
    text-transform: uppercase;
    letter-spacing: 0.2em;
    display: block;
    margin-bottom: 2rem;
  }

  .president-lead,
  .president-glass h2,
  .president-glass h3,
  .president-glass .artwork-card-title {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-weight: 600;
    color: rgb(var(--bright-cyan));
    line-height: 1.2;
    letter-spacing: -0.02em;
    margin: 0 0 2rem 0;
  }

  .president-lead {
    font-size: clamp(1.5rem, 2.5vw, 2.2rem);
    font-weight: 300;
    line-height: 1.5;
    color: rgb(var(--ice-blue));
  }

  .president-glass h2 {
    font-size: clamp(2.5rem, 4vw, 4rem);
  }

  .president-glass h3 {
    font-size: clamp(1.75rem, 2.5vw, 2.5rem);
  }

  .president-glass .artwork-card-title {
    font-size: clamp(1.5rem, 2vw, 2rem);
  }

  .president-glass p,
  .president-glass .artwork-card-description,
  .president-list li {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.05rem, 1.25vw, 1.25rem);
    line-height: 1.7;
    color: rgb(var(--ice-blue));
    font-weight: 400;
    margin-bottom: 1.5rem;
  }

  .president-glass p:last-child {
    margin-bottom: 0;
  }

  .president-intro-card,
  .president-interaction-card,
  .president-contact-card,
  .president-glass {
    padding: 4rem;
    background: rgba(var(--space-navy), 0.75);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border: 1px solid rgba(var(--bright-cyan), 0.3);
    border-radius: 16px;
    box-shadow:
      0 8px 32px rgba(var(--deep-space), 0.5),
      inset 0 1px 0 rgba(var(--ice-blue), 0.15),
      0 0 40px rgba(var(--teal-glow), 0.2);
    transition: all 0.3s ease;
  }

  .president-intro-card,
  .president-interaction-card,
  .president-contact-card {
    max-width: 1200px;
    margin: 0 auto;
  }

  .president-artwork-card-section .artwork-info-card.president-glass {
    background-image: linear-gradient(rgba(var(--space-navy), 0.82), rgba(var(--space-navy), 0.82)), url("/images/president_galaxy.webp");
    background-size: cover;
    background-position: center;
  }

  .president-glass:hover,
  .president-intro-card:hover,
  .president-interaction-card:hover,
  .president-contact-card:hover,
  .artwork-info-card.president-glass:hover {
    box-shadow:
      0 12px 40px rgba(var(--deep-space), 0.6),
      inset 0 1px 0 rgba(var(--ice-blue), 0.3),
      0 0 60px rgba(var(--teal-glow), 0.4);
    border-color: rgba(var(--bright-cyan), 0.5);
    transform: translateY(-2px);
  }

  .president-glass-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 3rem;
  }

  .president-what-is-this-card {
    display: flex;
    flex-direction: column;
  }

  .president-what-text {
    flex-shrink: 0;
  }

  .president-what-image-link {
    flex: 1 1 auto;
    min-height: 0;
    margin-top: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .president-what-image-link img {
    width: 100%;
    height: 100%;
    max-width: 100%;
    max-height: 100%;
    object-fit: cover;
    transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
  }

  .president-what-image-link:hover img {
    transform: scale(1.05);
  }

  @media (prefers-reduced-motion: reduce) {
    .president-what-image-link:hover img {
      transform: none;
    }
  }

  .president-list {
    list-style: none;
    padding: 0;
    margin: 1.5rem 0;
  }

  .president-list li {
    padding-left: 2rem;
    position: relative;
  }

  .president-list li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: rgb(var(--bright-cyan));
    font-weight: 400;
  }

  .president-fine-print {
    font-size: 0.95rem;
    opacity: 0.8;
    font-style: italic;
    margin-top: 2rem;
  }

  .president-rule {
    margin-bottom: 1.5rem;
  }

  .president-btn {
    background: linear-gradient(135deg, rgb(var(--bright-cyan)) 0%, rgb(var(--teal-glow)) 100%);
    color: rgb(var(--deep-space));
    padding: 1.25rem 3rem;
    border-radius: 8px;
    border: 1px solid rgba(var(--bright-cyan), 0.4);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    cursor: pointer;
    box-shadow:
      0 4px 16px rgba(var(--teal-glow), 0.3),
      inset 0 1px 0 rgba(255, 255, 255, 0.2);
    transition: all 0.3s ease;
  }

  .president-btn:hover {
    transform: translateY(-2px);
    box-shadow:
      0 6px 24px rgba(var(--teal-glow), 0.4),
      inset 0 1px 0 rgba(255, 255, 255, 0.3);
  }

  .president-portal-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.25rem, 4vw, 3.5rem);
    font-weight: 700;
    color: rgb(var(--bright-cyan));
    letter-spacing: -0.02em;
    margin-bottom: 2rem;
  }

  .president-portal-reach {
    font-family: 'Syne', sans-serif;
    font-size: clamp(1.1rem, 1.4vw, 1.35rem);
    font-weight: 600;
    color: rgb(var(--ice-blue));
    margin-bottom: 1.25rem;
    letter-spacing: 0.02em;
  }

  .president-portal-list {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
  }

  .president-portal-list li {
    font-family: 'Space Mono', monospace;
    font-size: clamp(0.95rem, 1.15vw, 1.1rem);
    color: rgba(var(--ice-blue), 0.95);
    padding: 0.6rem 0 0.6rem 1.5rem;
    position: relative;
    line-height: 1.5;
  }

  .president-portal-list li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: rgb(var(--bright-cyan));
    font-weight: 700;
  }

  .president-portal-meta {
    margin-bottom: 2rem;
  }

  .president-portal-meta p {
    font-family: 'Syne', sans-serif;
    font-size: clamp(1rem, 1.2vw, 1.15rem);
    color: rgb(var(--ice-blue));
    line-height: 1.6;
    margin-bottom: 0.75rem;
  }

  .president-portal-meta strong {
    font-family: 'Space Mono', monospace;
    font-size: 0.9em;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: rgb(var(--bright-cyan));
  }

  .president-portal-links {
    font-family: 'Space Mono', monospace;
    font-size: clamp(1rem, 1.15vw, 1.1rem);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    margin-bottom: 2rem;
  }

  .president-portal-links a {
    color: rgb(var(--bright-cyan));
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .president-portal-links a:hover {
    opacity: 0.75;
    text-decoration: underline;
  }

  .president-portal-sep {
    margin: 0 0.75rem;
    opacity: 0.5;
  }

  .president-portal-closing {
    font-family: 'Playfair Display', serif;
    font-size: clamp(1.2rem, 1.5vw, 1.4rem);
    font-style: italic;
    line-height: 1.6;
    color: rgba(var(--ice-blue), 0.92);
    margin: 0;
  }

  .president-cta {
    padding: 8rem 0;
    text-align: center;
    background: transparent;
  }

  .president-eden-headline {
    text-transform: uppercase;
    letter-spacing: -0.045em;
    line-height: 0.88;
  }

  .president-eden-body {
    font-size: clamp(1.4rem, 2.2vw, 2.2rem);
    line-height: 1.4;
    color: rgb(var(--ice-blue));
  }

  .president-eden-mono {
    margin-top: 2rem;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: clamp(1rem, 1.3vw, 1.3rem);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(var(--ice-blue), 0.7);
  }

  .president-censored {
    display: inline-block;
    position: relative;
    cursor: pointer;
    letter-spacing: 0.2em;
    color: rgba(var(--ice-blue), 0.6);
  }

  .president-censored::after {
    content: attr(data-reveal);
    position: absolute;
    left: 0;
    top: 0;
    opacity: 0;
    color: rgb(var(--bright-cyan));
    transition: opacity 0.2s ease;
  }

  .president-censored:hover::after,
  .president-censored:focus-visible::after {
    opacity: 1;
  }

  .president-retreat-btn {
    background: transparent;
    border: 1px solid rgba(var(--bright-cyan), 0.5);
    color: rgb(var(--bright-cyan));
    padding: 1.25rem 3rem;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.3s ease;
    border-radius: 4px;
  }

  .president-retreat-btn:hover {
    background: rgba(var(--bright-cyan), 0.15);
    color: rgb(var(--ice-blue));
    box-shadow: 0 0 30px rgba(var(--bright-cyan), 0.3);
  }

  @media (max-width: 1024px) {
    .president-glass-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .president-hero {
      height: 60vh;
    }
    .president-title {
      font-size: clamp(2.4rem, 8vw, 6.4rem);
    }
    .president-intro-section,
    .president-artwork-card-section,
    .president-content-grid-section,
    .president-interaction-section,
    .president-contact-section {
      padding: 5rem 0;
    }
    .president-intro-card,
    .president-interaction-card,
    .president-contact-card,
    .president-glass {
      padding: 3rem 2rem;
    }
  }

  @media (max-width: 480px) {
    .president-hero {
      height: 50vh;
    }
    .president-title {
      font-size: clamp(2rem, 9.6vw, 4.8rem);
    }
    .president-intro-card,
    .president-interaction-card,
    .president-contact-card,
    .president-glass {
      padding: 2.5rem 1.5rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .president-title {
      animation: none;
    }
    .president-glass:hover,
    .president-intro-card:hover,
    .president-interaction-card:hover,
    .president-contact-card:hover,
    .artwork-info-card.president-glass:hover {
      transform: none;
    }
    .president-btn:hover {
      transform: none;
    }
  }

  /* Nar-Mail Express° - Edinburgh magical map style */
  .narmail-eden-wrap {
    --eden-bg: #fcf8f9;
    --eden-text: #134252;
    --eden-text-muted: #627078;
    --eden-primary: #208c8d;
    position: relative;
    min-height: 100vh;
    background: var(--eden-bg);
  }

  .narmail-hero {
    position: relative;
  }

  .narmail-hero-image {
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    background: var(--eden-bg);
  }

  .narmail-hero-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    background: linear-gradient(to top, rgba(252, 248, 249, 0.95), transparent);
  }

  .narmail-hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .narmail-layout {
    width: 100%;
    color: var(--eden-text);
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    overflow-y: auto;
  }

  .narmail-headline {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
  }

  .narmail-mono {
    font-family: ui-monospace, "SF Mono", Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
  }

  .narmail-mono-gold {
    color: #a67c00;
    background: linear-gradient(
      to right,
      #a67c00 20%,
      #ce9e00 40%,
      #ffcc33 50%,
      #ce9e00 60%,
      #a67c00 80%
    );
    background-size: 200% auto;
    background-position: 0% center;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: narmail-gold-shine 8s linear infinite;
  }

  @media (prefers-reduced-motion: reduce) {
    .narmail-mono-gold {
      animation: none;
      background-position: 50% center;
    }
  }

  .narmail-main-content {
    max-width: 72rem;
    margin: 0 auto;
  }

  .narmail-section {
    padding: 4rem 0;
    border-bottom: 1px solid rgba(19, 66, 82, 0.12);
  }

  .narmail-section:last-of-type {
    border-bottom: none;
  }

  .narmail-h2 {
    font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
    font-size: clamp(1.8rem, 3.2vw, 3rem);
    font-weight: 500;
    color: var(--eden-text);
    margin: 0 0 1.5rem 0;
    letter-spacing: -0.035em;
    line-height: 0.9;
  }

  .narmail-h3 {
    font-size: clamp(1.4rem, 2.2vw, 2.2rem);
    font-weight: 500;
    color: var(--eden-text);
    margin: 2.5rem 0 1rem 0;
    letter-spacing: -0.02em;
  }

  .narmail-h4 {
    font-size: clamp(1.15rem, 1.5vw, 1.5rem);
    font-weight: 500;
    color: var(--eden-text);
    margin: 1.5rem 0 0.75rem 0;
  }

  .narmail-section p,
  .narmail-note {
    font-size: clamp(1.4rem, 2.2vw, 2.2rem);
    line-height: 1.4;
    color: var(--eden-text);
    margin: 0 0 1.5rem 0;
  }

  .narmail-note {
    font-style: italic;
    color: var(--eden-text-muted);
  }

  .narmail-list,
  .narmail-features {
    list-style: none;
    margin: 0 0 1.5rem 0;
    padding: 0;
  }

  .narmail-list li,
  .narmail-features li {
    font-size: clamp(1.4rem, 2.2vw, 2.2rem);
    line-height: 1.4;
    color: var(--eden-text);
    margin-bottom: 0.75rem;
    padding-left: 2.25rem;
    position: relative;
  }

  .narmail-list li::before,
  .narmail-features li::before {
    content: '✦';
    position: absolute;
    left: 0;
    color: #a67c00;
    background: linear-gradient(
      to right,
      #a67c00 20%,
      #ce9e00 40%,
      #ffcc33 50%,
      #ce9e00 60%,
      #a67c00 80%
    );
    background-size: 200% auto;
    background-position: 0% center;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: narmail-gold-shine 8s linear infinite;
  }

  @keyframes narmail-gold-shine {
    to { background-position: 200% center; }
  }

  @media (prefers-reduced-motion: reduce) {
    .narmail-list li::before,
    .narmail-features li::before {
      animation: none;
      background-position: 50% center;
    }
  }

  .narmail-blockquote {
    font-size: clamp(1.4rem, 2vw, 2rem);
    font-style: italic;
    line-height: 1.5;
    color: var(--eden-text);
    margin: 1.5rem 0;
    padding: 0 0 0 2rem;
    border-left: 4px solid var(--eden-primary);
  }

  .narmail-terms {
    margin: 0 0 1rem 0;
    padding-left: 1.5rem;
    color: var(--eden-text);
    font-size: clamp(1.2rem, 1.6vw, 1.6rem);
    line-height: 1.6;
  }

  .narmail-terms li {
    margin-bottom: 1.25rem;
  }

  .narmail-guarantee .narmail-blockquote {
    font-size: clamp(1.5rem, 2.2vw, 2.2rem);
  }

  .narmail-image-above-footer {
    position: relative;
    width: 100%;
    min-height: 60px;
    background: var(--eden-bg);
    padding-bottom: 0;
  }

  .narmail-footer {
    width: 100%;
    background: #000;
    color: #fff;
    padding: 8rem 0;
    position: relative;
    z-index: 10;
  }

  .narmail-q-a-image {
    position: absolute;
    bottom: -0.78rem;
    right: 2rem;
    max-width: clamp(100px, 16vw, 180px);
    height: auto;
    object-fit: contain;
  }

  .narmail-footer-scatter {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem 2rem;
    max-width: 72rem;
    margin: 0 auto;
  }
  .narmail-footer-line {
    margin: 0;
    line-height: 1.4;
    color: rgba(255, 255, 255, 0.9);
  }
  .narmail-footer-headline {
    font-family: 'Playfair Display', serif;
    font-size: 2.25rem;
    font-weight: 700;
    grid-column: 1 / -1;
    text-align: center;
    color: #fff;
  }
  .narmail-footer-dreams {
    font-family: 'Syne', sans-serif;
    font-size: 1.2rem;
    font-style: italic;
    color: rgba(255, 255, 255, 0.8);
    grid-column: 1 / -1;
    text-align: right;
    transform: rotate(-1deg);
  }
  .narmail-footer-invest {
    font-family: 'Bungee', cursive;
    font-size: 1.1rem;
    color: #fff;
    grid-column: 1;
  }
  .narmail-footer-link-wrap {
    grid-column: 2;
    transform: rotate(1deg);
  }
  .narmail-footer-link {
    font-size: 1.1rem;
    background: linear-gradient(to right, #a67c00 20%, #ce9e00 40%, #ffcc33 50%, #ce9e00 60%, #a67c00 80%);
    background-size: 200% auto;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-decoration: underline;
    text-underline-offset: 0.2em;
    animation: narmail-shine 8s linear infinite;
  }
  .narmail-footer-link:hover {
    opacity: 0.9;
  }
  .narmail-footer-what {
    font-family: 'Bungee', cursive;
    font-size: 1.05rem;
    color: rgba(255, 255, 255, 0.75);
    grid-column: 1 / -1;
    margin-top: 0.5rem;
  }
  .narmail-footer-check {
    font-family: 'Syne', sans-serif;
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.9);
  }
  .narmail-footer-check:nth-child(6) { grid-column: 1; }
  .narmail-footer-check:nth-child(7) { grid-column: 2; transform: rotate(-0.5deg); }
  .narmail-footer-check:nth-child(8) { grid-column: 1; transform: rotate(0.5deg); }
  .narmail-footer-price {
    font-family: 'Space Mono', monospace;
    font-size: 1.05rem;
    color: rgba(255, 255, 255, 0.7);
    grid-column: 1 / -1;
    text-align: center;
    margin-top: 1rem;
  }
  .narmail-footer-contact {
    font-family: 'Space Mono', monospace;
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.7);
    grid-column: 1 / -1;
    text-align: center;
    margin-top: 0.5rem;
  }
  .narmail-footer-contact a {
    color: #fff;
    text-decoration: none;
  }
  .narmail-footer-contact a:hover {
    text-decoration: underline;
  }
  .narmail-footer-sep {
    margin: 0 0.5rem;
    opacity: 0.6;
  }
  .narmail-footer-cta {
    grid-column: 1 / -1;
    margin-top: 2rem;
    text-align: center;
  }
  @media (max-width: 640px) {
    .narmail-footer-scatter {
      grid-template-columns: 1fr;
    }
    .narmail-footer-check:nth-child(6),
    .narmail-footer-check:nth-child(7),
    .narmail-footer-check:nth-child(8) {
      grid-column: 1;
    }
    .narmail-footer-dreams { transform: none; }
    .narmail-footer-link-wrap { transform: none; }
    .narmail-footer-check { transform: none !important; }
  }
  @media (prefers-reduced-motion: reduce) {
    .narmail-footer-line { transform: none !important; }
    .narmail-footer-link { animation: none; }
  }

  .narmail-retreat-btn {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.75rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.7);
    background: transparent;
    padding: 0.5rem 1rem;
    border-radius: 4px;
    border: 1px solid rgba(255, 255, 255, 0.3);
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .narmail-retreat-btn:hover {
    color: #fff;
    border-color: rgba(255, 255, 255, 0.6);
  }

  .narmail-artwork-card-section {
    padding: 5rem 0;
  }

  .artwork-info-card.narmail-glass {
    background: #0a0a0a;
    border: 1px solid #3a3a3a;
    max-width: 960px;
    margin: 0 auto;
    position: relative;
    overflow: hidden;
  }

  .narmail-artwork-card-section .artwork-info-card.narmail-glass {
    background-image: linear-gradient(rgba(10, 10, 10, 0.92), rgba(10, 10, 10, 0.92)), url("/images/nar_mail_artwork.webp");
    background-size: cover;
    background-position: center;
  }

  .artwork-info-card.narmail-glass::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(206, 158, 0, 0.08), transparent);
    transition: left 0.6s ease;
  }

  .artwork-info-card.narmail-glass:hover {
    border-color: rgba(206, 158, 0, 0.5);
    box-shadow: 0 16px 56px rgba(0, 0, 0, 0.5), 0 0 0 1px rgba(206, 158, 0, 0.15);
    transform: translateY(-2px);
  }

  .artwork-info-card.narmail-glass:hover::before {
    left: 100%;
  }

  .narmail-glass .artwork-card-title {
    background: linear-gradient(to right, #a67c00 20%, #ce9e00 40%, #ffcc33 50%, #ce9e00 60%, #a67c00 80%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 200% auto;
    background-position: 0% center;
    animation: narmail-shine 8s linear infinite;
    color: transparent;
  }

  @keyframes narmail-shine {
    to { background-position: 200% center; }
  }

  @media (prefers-reduced-motion: reduce) {
    .narmail-glass .artwork-card-title {
      animation: none;
      background-position: 50% center;
    }
  }

  .narmail-glass .artwork-card-title::before {
    content: '// GALLERY';
    display: block;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.65rem;
    font-weight: 500;
    color: rgba(206, 158, 0, 0.7);
    letter-spacing: 0.2em;
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    -webkit-text-fill-color: initial;
  }

  .narmail-glass .artwork-card-description {
    color: rgba(255, 255, 255, 0.88);
    font-weight: 400;
  }

  .narmail-glass .artwork-card-icon.narmail-icon {
    background: rgba(206, 158, 0, 0.15);
    color: #ce9e00;
  }

  .artwork-card-button.narmail-btn {
    background: transparent;
    color: #ce9e00;
    border: 2px solid #ce9e00;
  }

  .artwork-card-button.narmail-btn:hover {
    background: #ce9e00;
    color: #0a0a0a;
    box-shadow: 0 4px 24px rgba(206, 158, 0, 0.35);
  }

  @media (max-width: 768px) {
    .narmail-artwork-card-section {
      padding: 3rem 0;
    }
    .narmail-artwork-card-section .artwork-card-content {
      flex-direction: column;
      text-align: center;
      padding: 2rem;
    }
    .narmail-artwork-card-section .artwork-card-button {
      width: 100%;
      justify-content: center;
    }
    .narmail-footer {
      padding: 6rem 0;
    }
    .narmail-footer-headline {
      font-size: 1.875rem;
    }
    .narmail-section {
      padding: 3rem 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .narmail-retreat-btn {
      transition: none;
    }
  }
</style>
